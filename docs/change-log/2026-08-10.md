# 2026-08-10 修改总结

- [03:00] 形状建造架构重构：引入 `BuildShape`/`ShapeInput` 一等公民抽象，统一渲染与提交派发，修复球形状 RADIUS 阶段右键无法确认的断点；`LineBrushSelector` 退化为纯状态机，清理 6 个 `isXxxBuildActive` 桥接与死代码（涉及 `client/build/shape/`、`LineBrushSelector`、`LineBrushRenderPass`、`BuildInteractionHandler`、`LeftSidebarPanel`、`ShapeButtonGroup`）。
- [03:10] 连锁挖掘修复：`RtsUltimineProcessor` 各提前返回/创造模式路径补发 `sendUltimineProgress(-1,0)`，修复"有时需挖两次"（客户端 `activePos` 残留误判为取消）。
- [03:20] RTS 模式破坏静音：新增 `ClientLevelMixin`（拦破坏粒子 `addDestroyBlockEffect`）。
- [03:30] 音效体系重构：破坏音改为客户端本地主相机播放（`RtsClientNetworkHandlers.handleBreakAnimation`，限流 1/tick），修复跨端相机坐标不同步导致的"听不到/靠方块才听见"；`destroyMinedBlock` 与 `RtsPlacedRecoveryService` 统一走 `sendBreakAnimation`；删除死方法 `RtsPlacementSound.playRemoteBlockBreakSound`；新增 `ClientPacketListenerMixin` 拦 2001 破坏事件。
- [03:45] docs 前端化：新增 Vue3+Vite SPA（`docs/app/`），报告改为 JSON 数据驱动（`docs/schemas/logic-review.schema.json` + `docs/reports/*.json`，迁移 6 份历史报告）；`AGENTS.md` 链路检查流程改为产出 JSON，并加入"每日修改总结"记忆规则（本文件）；历史 HTML 报告统一为模板格式并保留在 `docs/`。
- [03:55] 前端新增"工作日志"视图：`App.vue` 顶部 tab 切换「链路检查报告 / 工作日志」，新增 `WorkLog.vue` 读取 `docs/change-log/*.md` 用 marked 渲染并按日期倒序展示（引入依赖 `marked`）。
- [04:10] 破坏侧形状预览渲染区分：`LineBrushRenderPass` 新增 `lineBrushBreakColor`（红色）字段，破坏侧预览可破坏方块用红色高亮、不可破坏方块（破坏速度<0）用紫色提示，与放置侧蓝色/紫色区分（复用 BuildShape 的破坏侧画笔链路已存在，本次完善视觉反馈）。
- [04:20] 破坏侧预览空气位置不再跳过：`LineBrushRenderPass` 新增 `lineBrushAirColor`（灰色），破坏侧空气位置以灰色渲染形状覆盖范围，与红色（可破坏）/紫色（不可破坏）区分。
- [04:30] 破坏侧画笔改左键选取：`BuildInteractionHandler.tryStartLineBrush` 增加 `leftClick` 参数，建造侧由右键驱动、破坏侧由左键驱动（选起点/终点/确认），右键在破坏侧形状激活时消费防相机；`handleMouseRelease` 补充左键画笔激活消费；`LineBrushSelector.currentHint` 破坏侧提示把"右键"替换为"左键"。
- [05:00] 放置/破坏渲染链路走查修复：P1 放置动画断点——新增 `PlaceAnimationPass` 消费 `GhostRingBuffer`（白色线框 500ms 淡出）并注册到 `RenderPipeline`，修正 `GhostRingBuffer.forEach` 全槽遍历与 `prune` 计数；P2 新增 `ActionRadiusFilter` 工具接入 `LineBrushRenderPass`/`UltiminePreviewPass` 形状预览动作半径过滤（与服务端对齐）；P3 清理 `LeftSidebarPanel`/`BuildInteractionHandler` 过时"尚未实现"注释；产出报告 `docs/reports/place-break-render-chain.json`。
- [05:40] 线框渲染性能优化（放置/破坏形状预览卡顿）：新增 `OutlineEdgeExtractor`（O(n) 外表面面单元区域边界提取 + 共线合并，含 `OutlineEdgeExtractorTest` 6 用例），`LineBrushRenderPass` 改按颜色分组 + 轮廓边渲染（顶点量从 O(n)×576 降到 O(轮廓边)），`LineBrushSelector.computePositions` 增加输入 stamp 缓存避免每帧重算大形状，`RenderPipeline` brackets/noDepth 缓冲 128KB→512KB。
- [06:00] 放置/破坏特效 pass：放置——`PlaceAnimationPass` 重写为"方块从天降落建造"（半透明方块从 y+8 格降落到目标后淡出，颜色取方块 MapColor）；破坏——新增 `BreakEffectPass`（方块碎块 6 个从原位确定性方向上飘散淡出），`RingBufferHolder` 新增 `BREAK_EFFECTS` 缓冲，`handleBreakAnimation` 写入破坏前方块状态。
- [06:15] 放置特效调整：降落高度 8→3→2 格；渲染形式从实心方块改为角支架线框（保留下落动画 + MapColor 着色，深度 + 穿透双层）。
- [06:40] 破坏动画"不播放"链路走查修复：根因 `GhostRingBuffer` 容量 32 太小 + 满时滚动覆盖挤掉播放中动画。重构 `GhostRingBuffer`（可配置容量、满时覆盖最旧、`add`/`forEach` 全槽去重与遍历），`BREAK_EFFECTS`=128、`INSTANCE`=64；新增 `GhostRingBufferTest` 4 用例；产出报告 `docs/reports/place-break-effect-chain.json`。
- [07:10] 破坏动画"不渲染"二次修复：根因写入速率（区域破坏 64/tick≈1280/秒）远超缓冲承受能力（128/600ms≈213/秒），大量条目渲染前被覆盖。`handleBreakAnimation`/`handlePlaceAnimation` 按 tick 限流写入（每 tick 20 个），`GhostRingBuffer.add` 满时丢弃新条目（保护已入缓冲动画），`BREAK_EFFECTS`/`INSTANCE` 容量提升到 256；更新报告与测试。
- [07:30] 破坏动画改为线框碎块：`BreakEffectPass` 从实心半透明碎块（`renderFilledFaces`）改为角支架线框碎块（`renderCornerBrackets` 深度 + 穿透双层），碎块数量 6→3。
- [07:40] 破坏动画改回实心半透明碎块：`BreakEffectPass` 恢复 `renderFilledFaces` 渲染，保留碎块数量 3（减少粒子）。
