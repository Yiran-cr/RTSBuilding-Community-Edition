# 2026-08-24

- [14:40] 科技端只保留火力发电机：移除储能单元/无线节点/无线充电站/多方块占位方块及其方块实体、菜单、网络包、无线频道管理器，并精简注册表（RtsEnergyBlocks/BlockEntities/Items/CreativeTabs/Capabilities/Client/Mod/GameEvents）与 lang、资源文件、主模组 Config（wirelessNodeRate/wirelessChargerRate）。AGENTS.md 架构索引同步更新。

- [15:17] 删除玩家能量网格机制，新增戴森球式无线输电塔（power_tower）：移除 api 的 RtsEnergyAPI/RtsAPI.energy()、main 的 RtsBuildEnergy/RtsAPIImpl.setEnergyApi/RtsPlacementBatch 耗能调用/Config.energyPerPlacement、technologized 的 RtsEnergyNetworkManager/ApiImpl/CostService/Node/GameEvents/RtsEnergyBlockEntity（网格+owner 生命周期）。新增 RtsPowerTowerBlock(Entity)（自带缓冲+范围吸取+范围分发，分片扫描/限流轮转，Config 新增 powerTowerCapacity/HorizontalRadius/VerticalRadius/TransferRate）及资源/lang，热能发电机改为纯 extract-only 缓冲源。AGENTS.md 同步更新。

- [15:31] 新增储能单元（energy_cell）：RtsEnergyCellBlock(Entity) 大容量 FE 缓冲（Config.energyCellCapacity 默认 400 万），双向 IEnergyStorage 可被输电塔当源/目标或管道充放，右键 actionbar 显示电量（message.rtsbuilding_technologized.energy_cell_info）。注册/资源/lang/粒子挂载齐备，清理无引用的 grid_no_energy lang key。AGENTS.md 同步更新。

- [15:37] 输电塔搬运升级为公平按需调度（RtsPowerTowerBlockEntity）：分发按各目标需求余量占比累计配额、提取按各源存量占比累计配额，并维护欠账 credit，每 tick 优先服务欠账最高的源/目标（min(credit,余量/存量,缓冲,预算)），长期公平不饿死、不硬塞、不掏空单一源。AGENTS.md 同步更新。

- [15:55] 科技端改名"星球崛起 Planet Rise"（Gradle 项目 rtsbuilding-technologized→rtsbuilding-planetrise、modId rtsbuilding_technologized→rtsbuilding_planetrise、Java 包 com.rtsbuilding.rtsbuilding.energy→planetrise），同步更新 resources 路径/资源内容/build 配置（settings.gradle/gradle.properties/main build.gradle addonManifest/toml/.idea）与 AGENTS.md。主模组 Config.enableTechnologized 配置键保留以兼容玩家配置。

- [16:20] planetrise 模块全部类移除 Rts 前缀（RtsEnergyMod→EnergyMod、RtsEnergyBlocks→EnergyBlocks、RtsThermalGeneratorBlock→ThermalGeneratorBlock、RtsPowerTowerBlockEntity→PowerTowerBlockEntity 等 16 个，ContainerEnergyStorage 原本无前缀），同步更新 main build.gradle addonManifest 入口类与 AGENTS.md 架构索引。

- [16:45] 贡献者信息重排：作者改为五世桃花亭（gradle.properties mod_authors、ui toml authors），贡献者 JerryLunar/Diffusion/ReConstruction-127/可怜Bot/卓清婉/凌墨问（main toml 7 处 credits、ui toml credits、gradle.properties mod_credits），README 中英文贡献者段移除 Yiran/Eternal-Snowstorm 新增 Diffusion，版权行作者同步。构建+测试+打包校验通过。

- [17:10] 重写全部模组包 toml 模板（main 与 ui 的 neoforge.mods.toml）：统一格式与字段顺序、补齐 displayURL、增加分区注释；修复注释行含非法占位符（${...}）导致 SimpleTemplateEngine 解析失败的问题，改为纯 ASCII 注释。构建+测试+verifyAddonPackaging 通过。

- [18:20] planetrise 落地"主方块 + 不可见占位方块"多占位机制（参考 Mekanism IBoundingBlock/BoundingBlock）：新增 IBoundingBlock 接口（占位偏移/创建/清除/形状/交互转发，含先移除实体防递归防护）、BoundingBlockEntity（mainPos NBT 持久化）、BoundingBlock（shape 按相对偏移平移代理、右键/破坏/拾取/挖掘进度转发主方块、RenderShape.INVISIBLE）。实现风力发电机 wind_generator：WindGeneratorBlock（1 主方块 + 上方 4 占位，onPlace 创建占位+失败回滚、onRemove 清除，两端各自放置保证客户端占位一致）、WindGeneratorBlockEntity（按塔顶高度比例产电 + canSeeSky，extract-only 缓冲供热电塔吸取）、客户端 ModelWindGenerator（Java 模型平移绘制整塔+风叶旋转动画）+ RenderWindGenerator（BEWLR，translate(0.5,0,0.5) 跨 5 格绘制）。注册/能力（extract-only energy）/创造栏/破坏粒子聚合/资源（blockstate/model/lang/loot/生成金属贴图）齐备，主模组 Config 新增 windGeneratorCapacity/MinGeneration/MaxGeneration。构建+测试+verifyAddonPackaging 通过。AGENTS.md 未更新（本次为示例性质，未改架构约定）。

- [18:50] 修复风力发电机模型倒置：ModelWindGenerator 误按实体模型约定（Y 向下为正）写负 Y 坐标，而 BEWLR 无翻转时 ModelPart Y 正方向即世界 Y 正方向（向上），导致塔倒着长到地下。改为方块坐标系（Y 向上为正）：底座 y 0..3、塔杆 y 3..76、机舱 y 76..84、风叶绕 y 82 旋转；渲染器无需翻转。碰撞 SHAPE 本就用 VoxelShape 方块坐标系，正确无需改。

- [19:10] 风力发电机物品模型改为 BEWLR 渲染（参考 Mekanism RenderWindGeneratorItem）：新增 RenderWindGeneratorItem（BlockEntityWithoutLevelRenderer，物品栏/手持/掉落均渲染完整 Java 塔模型，风叶随游戏时间旋转，资源重载重建模型），EnergyClient 注册 reload listener + IClientItemExtensions.getCustomRenderer；models/item/wind_generator.json 改为 builtin/entity + Mekanism 调校的 display 变换。

- [19:40] 深入对照 Mekanism 完整占位机制修复 BoundingBlock：① 新增 onDestroyedByPlayer——willHarvest=true（生存挖掘）时不 setBlock(air) 直接 return true，避免占位实体在 playerDestroy 转发前被移除导致主方块掉落丢失（核心掉落修复）；② 新增 playerWillDestroy/onExplosionHit 转发主方块（事件/粒子/爆炸掉落正确）；③ 新增 canBeReplaced 允许替换孤儿占位；④ 新增 neighborChanged 转发红石到主方块；⑤ IBoundingBlock 增加 getBoundingEnergyStorage + WindGeneratorBlockEntity 实现 + EnergyCapabilities 为 BOUNDING_BLOCK 注册能量能力代理，塔身占位格子也可被输电塔/管道取能量。编译+完整 build 通过。

- [20:00] 修复客户端启动崩溃（ExceptionInInitializerError: No model for layer rtsbuilding_planetrise:wind_generator#main）：RenderWindGeneratorItem 静态单例构造器里立即 bakeLayer，但 RegisterLayerDefinitions 事件晚于 RegisterClientReloadListenersEvent 触发，layer 未注册。改为构造器不烘焙、仅 onResourceManagerReload（首次资源重载时 layer 已注册）烘焙 + renderByItem 防御性懒加载，与 Mekanism MekanismISTER 一致。

- [20:30] 修复风力发电机占位格子无碰撞/无选择箱（玩家站上占位格会掉落、"弹"）：根因是客户端占位方块 mainPos 未绑定——placeBoundingBlocks 里 setBlockAndUpdate 在 p 已是占位（服务端同步先到）时因同 state 返回 false，且 getBlockState(p).canBeReplaced() 对占位方块返回 false 提前 continue，导致客户端占位实体 mainPos 恒为 null，getShape/getCollisionShape 走 Shapes.empty()。改为幂等绑定：目标空气/可替换才放置占位，非占位占用才判失败，无论是否新放置都 setMainLocation。补 BlockState import。

- [21:10] 补齐占位 mainPos 服务端兜底同步（对照 Mekanism syncMasterPosition/syncMasterToBounding）：① IBoundingBlock 新增 syncBoundingBlocks()——服务端遍历所有占位强制重发实体更新包；② WindGeneratorBlockEntity 新增 syncMasterToBounding 标志，tickServer 首次 tick 调用 syncBoundingBlocks，解决 RTS 远程放置时客户端 onPlace 未执行/实体更新包时序竞态（包到达时占位实体未创建被丢弃）导致客户端占位 mainPos 恒 null、占位格无碰撞无选择箱的残余问题；③ BoundingBlockEntity 显式重写 getUpdateTag/handleUpdateTag 确保 mainPos 必走实体更新包（参照 Mekanism TileEntityBoundingBlock）；④ 移除 BoundingBlock.proxyShape 每帧 DEBUG 日志（getShape/getCollisionShape 高频调用导致日志刷屏，Mekanism 无此日志）。编译+测试+完整 build 通过。

- [21:50] 新增「Java 模型 ↔ 碰撞箱同源」工具（参考 RtsModelShapeParser 从 JSON 生成碰撞箱的做法，为 Java 模型提供等价物）：① common 层新增 RtsJavaModelShape（纯几何盒数据，方块像素坐标 addBox + buildShape() 服务端安全生成 VoxelShape，附单测 RtsJavaModelShapeTest）；② client 层新增 JavaModelShapeUtil——toCubeListBuilder(RtsJavaModelShape, origin)（几何数据→渲染 CubeListBuilder）与 fromModelPart(ModelPart, ignoreParts)（已烘焙 ModelPart 8 角点矩阵变换→VoxelShape）；③ WindGeneratorBlock 改为 MODEL（RtsJavaModelShape）生成 SHAPE，ModelWindGenerator 塔身用 JavaModelShapeUtil.toCubeListBuilder(MODEL, 8,0,8) 同源构建，风叶（旋转部件）不参与碰撞单独构建；④ AGENTS.md 登记新工具类并明确规范：所有 Java 模型方块碰撞箱用 RtsJavaModelShape.buildShape()、渲染用 toCubeListBuilder，禁止手写 Shapes.or(Block.box(...))。编译+测试+完整 build 通过。









