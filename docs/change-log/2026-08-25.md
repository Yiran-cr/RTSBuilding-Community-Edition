# 2026-08-25 修改总结

- [12:00] 风力发电机换用用户 Blockbench 新模型/贴图（Wind_Turbine）：ModelWindGenerator 照搬导出几何（128×128 UV），风叶 bone 组绕 z 轴旋转动画，渲染 scale(1,-1,1)+translate(0,1.5,0) 翻转实体坐标落位；贴图 wind_generator.png 缩放为 128×128（与 UV 匹配）。
- [12:05] 塔高 5 格改 3 格：WindGeneratorBlockEntity.BOUNDING_HEIGHT 4→2、BOUNDING_OFFSETS 缩为 above(1/2)；WindGeneratorBlock.MODEL 碰撞改为 3 格塔身（底座 y0..5 / 塔杆 y5..30 / 顶部 y30..48）；RenderWindGenerator 包围盒改 3 格，注释更新。
- [12:10] AGENTS.md planetrise 节同步（塔身 3 格占位 2、模型来源、BEWLR 变换说明）。
- [12:20] 修复风力发电机模型"反的"：ModelWindGenerator.render 由 scale(1,-1,1)（仅翻 Y，镜像）改为 scale(-1,-1,1)（等价绕 Z 轴 180°，X/Y 同翻，与 Mekanism RenderWindGenerator 一致），消除贴图镜像颠倒。
- [12:30] 风叶旋转轴改为绕 y 轴：ModelWindGenerator.render 由 bone.zRot 改为 bone.yRot（竖直方向为轴心），注释与 AGENTS.md 同步。
- [12:40] 修复模型整塔落到地下：ModelWindGenerator.render 的 scale(-1,-1,1) 与 translate(0,1.5,0) 顺序颠倒（PoseStack 右乘，后调用先作用于顶点）——先 translate 抬到地面再 scale 翻转，与 Mekanism translate→mulPose(ZP180) 一致。
- [12:50] 碰撞箱贴合模型：WindGeneratorBlock.MODEL 由 3 段简化盒细化为 11 段（底座/中层收窄/塔杆/装饰环/塔顶），自动 buildShape() 生成碰撞。
- [12:55] 沉淀规范到 AGENTS.md 第六节：新增「Blockbench Java 模型方块（LayerDefinition）建模规范」——flip_y 坐标翻转、BEWLR translate/scale 顺序、旋转部件 yRot 与不参与碰撞、碰撞箱必须纯数据 RtsJavaModelShape.buildShape()（禁 fromModelPart）、占位高度与模型格高一致。
- [13:10] 无线输电塔模型改为 Blockbench 新模型（Wireless_Energy_Transmission_Tower，5 格高）：新增 ModelPowerTower/RenderPowerTower/RenderPowerTowerItem，贴图缩放到 128×128；PowerTowerBlock 由普通方块改为多占位（1 主方块 + 上方 4 占位），MODEL 碰撞 8 段贴合塔身；PowerTowerBlockEntity 实现 IBoundingBlock（占位/代理形状/占位格子可取能量/首 tick 兜底同步）；EnergyClient 注册渲染器/图层/物品渲染器，item JSON 改 builtin/entity；AGENTS.md 同步。
- [13:20] 能量塔模型完全依照导出代码核对（36 盒逐盒一致），碰撞箱补全四角柱细节（MODEL 由 8 段扩为 12 段）。
- [13:30] 能量塔顶部天线动画：ModelPowerTower.render 按 Blockbench 动画驱动 rotate 组（3 秒绕 y 轴转一圈 + 1.0→0.8→1.0 缩放脉冲），RenderPowerTower(Item) 传游戏时间秒。
- [14:05] 输电塔能量光束特效：PowerTowerBlockEntity 记录实际吸取/分发的源/目标坐标，每 4 tick 经新 S2CEnergyBeamPayload 节流广播给塔 256 格内玩家；新增 EnergyBeamRenderer 客户端渲染（蓝线吸取源顶↔塔顶、绿线分发塔顶↔目标顶，白色核心+偏移光晕+流动能量光点+端点光晕），EnergyMod 注册 payload、EnergyClient 挂 RenderLevelStageEvent。
- [14:30] 光束改粒子特效（科技感）：新增 EnergyParticleTypes（energy_beam 粒子类型 + DeferredRegister 注册）、EnergyBeamParticle（发光菱形晶片粒子，半透明满亮度渲染，正弦脉动闪烁+尾段淡出）、EnergyBeamParticleProvider（注入 SpriteSet 到渲染器）；EnergyBeamRenderer 改为客户端 tick 每链路每 tick 生成 2 个粒子（吸取蓝源顶→塔顶/分发绿塔顶→目标顶，速度 13 格/秒，相位随机错开）；EnergyClient 改用 ClientTickEvent.Pre 驱动、注册粒子 provider；新增粒子贴图 assets/rtsbuilding_planetrise/textures/particle/energy_beam.png（32×32 发光菱形十字晶片）。
- [15:40] 修复粒子特效 NPE 崩溃（ParticleEngine$MutableSpriteSet.get: sprites 为 null）：补上缺失的 particles/energy_beam.json（registerSpriteSet 粒子必须有匹配粒子 JSON，reload 时才 rebind 贴图列表）；EnergyBeamRenderer.tick 对粒子构建加 try-catch 容错（贴图集未就绪时本帧跳过，不崩客户端）。
- [16:12] 修复输电塔能量粒子会越过目标方块：EnergyBeamParticle 构造里 flyTicks（推进）与 totalTicks（flyTicks+8 淡出）共用步进，导致粒子到终点后仍多飞 8 tick；改为 age<flyTicks 才推进、到达终点后停在原地用 FADE_TICKS 线性淡出，不再越块（rtsbuilding-planetrise/client/particle/EnergyBeamParticle.java）。
- [16:18] 修复输电塔能量粒子重复/不连方块顶部：多占位方块（塔/风力机/储能）主方块+占位格经 IBoundingBlock 代理暴露同一缓冲，塔 A 扫描时把塔 B 的每个格子都当独立源/目标 → 同一方块多条重复射线、端点落在占位格顶部。PowerTowerBlockEntity 新增 normalizeMain() 将占位格归一化为主方块坐标并统一判重入列，activePull/activePush 广播前即可生成重复坐标。
- [16:21] 优化输电塔每 tick 性能：a) 新增 storageCache（位置→能力对象 + 方块实体归属校验），findStorage 一次查询后每 tick 仅 getBlockEntity 校验复用，避免扫描+搬运对每个源/目标反复做昂贵 capability 查询；b) scanSlice 在源/目标列表均达上限时整段跳过扫描（失效项仍由搬运侧清理）。
- [16:28] 移除输电塔能量传输粒子特效相关全部逻辑：删文件 EnergyBeamRenderer/EnergyBeamParticle/EnergyBeamParticleProvider（client/particle、client/render）、EnergyParticleTypes（particle）、S2CEnergyBeamPayload（network）、EnergyClientPayloadDispatcher（client/network）及资源 particles/energy_beam.json、textures/particle/energy_beam.png；PowerTowerBlockEntity 移除 activePull/activePush/beamSyncCounter 与 syncBeamsIfNeeded 广播；EnergyClient 移除 onRegisterParticleProviders/onClientTick；EnergyMod 移除 EnergyParticleTypes.register 与 registerPayloads；清理空包目录。
- [17:20] 新增所有 RTS 能量机器抽象基类 AbstractEnergyMachineBlockEntity（rtsbuilding-planetrise/block/entity）：统一能量缓冲（BasicEnergyContainer）、createEnergyStorage 能力工厂、addEnergy/consumeEnergy 内部充放电、tickServer/tickClient 模板（level/client/Config 守卫 + 首次占位同步）、energy NBT 持久化 + saveEnergyAdditional/loadEnergyAdditional 扩展钩子。
- [17:25] 迁移现有 Energy 机器到该基类：ThermalGeneratorBlockEntity（岩浆罐移入扩展钩子、tickServer→onServerTick、addEnergy 产电）、EnergyCellBlockEntity（纯存储，继承即用）、PowerTowerBlockEntity（删 buffer/markChanged/getBuffer/syncMasterToBounding/NBT，改 createEnergyStorage(双向)+addEnergy/consumeEnergy+onServerTick）、WindGeneratorBlockEntity（同理，clientTick→tickClient）；WindGeneratorBlock.getTicker 改调 tickClient。
- [17:30] AGENTS.md 第三节 planetrise block.entity 行同步（加入基类说明与新迁移机器描述）。
- [18:05] 修复无线输电塔跳过 Mekanism 机器：根因——Mekanism 对 side=null（任意面）返回只读代理（ProxyStrictEnergyHandler，EnergyHandlerManager.resolve 对 null 面构造），其 canExtract/canReceive 恒 true 但 insert 全拒/extract 返回 0；原 PowerTowerBlockEntity.queryStorage 用 side=null「命中即返回」故拿到只读对象，目标模拟注入=0 被跳过、源模拟提取=0 提不动。修复：findStorage → findUsableStorage(pos, wantExtract)，以「模拟 extract/receive > 0」为真实可交互判据 + 任意面→6 面回退避开只读代理（PROBE_SIDES，canInteract 辅助），scanSlice/pullFromSources/pushToTargets 全部改用按用途探测。
- [19:40] **重设计科技端电力系统**（参照 power_system_design.md，彻底摒弃旧「戴森球式塔内吸取+分发」）：新增 power 包（PowerRole/GridNode/PowerScheduler/IPowerGridNode/PowerGridManager）——PowerScheduler 纯算法做 union-find 组网「发电-发电不直连」→ 电网聚合发电量与需求 → 按需求占比+吞吐封顶分配配额（转发不衰减）；PowerGridManager 服务端每维度惰性调度、写回各塔 acceptQuota。PowerTowerBlockEntity 重写为电网输电塔（链路范围+供电范围、分片扫描 demand、broadcast 广播注能）；Thermal/WindGenerator 实现 IPowerGridNode 发电节点上报 generation；EnergyCell 归为用电/存储节点；AbstractEnergyMachineBlockEntity 统一 onLoad/setRemoved 电网注册注销；Config 改为 powerTowerLinkRange/PowerRange/Throughput + generatorLinkRange。新增客户端范围圈视觉（client/power：PowerRangeVisualStore + PowerRangeOverlayRenderer + EnergyClient 快捷键 P 全局显示链路蓝圈/供电黄圈）。新增 PowerSchedulerTest（8 用例）+ main testImplementation planetrise。AGENTS.md 3.5 / 数据流同步。


