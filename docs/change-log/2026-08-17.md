# 2026-08-17

- [10:00] uifw 全面去 RTS 化重命名（大工程）：包 `com.rtsbuilding.rtsui`→`com.rtsbuilding.uifw`、mod id/资源命名空间 `rtsbuilding_ui`→`uifw`、`RtsPanel`→`UiPanel`、`RtsPanelApi`→`UiPanelApi`、`RtsPanelHost`→`UiPanelHost`、`RtsFloatingWindowLayer`→`FloatingWindowLayer`、`RtsButton`→`UiButton`、`RtsShaders`→`UiShaders`、`RtsUiMod`→`UiFrameworkMod`、`DarkUiPalette`→`UiPalette`、`getRtsWidth/Height`→`getUiWidth/Height`、`enableRtsScissor`→`enableUiScissor`；118 文件批量替换 + 包目录/类文件/`assets/uifw` 目录重命名，主 mod 100+ import 同步，全仓无旧名残留。
- [10:30] 业务移出：`FeatureAdjusterState`（漏斗半径/连锁挖掘）从 uifw 移到主 mod `com.rtsbuilding.rtsbuilding.client.state`，主 mod 5 处调用点 import 更新；`ShaderState`（Iris/OptiFine 检测，全库零调用死代码）删除。
- [10:30] 分层修复：`PanelDragPerformanceOptimizer` 从 `render` 移到 `window.window.handler`（消除 render→window 反向依赖）；UI 库注释/RTS 字样清理（`UiFrameworkMod`、`UiPanelHost`、输入框注释、异常消息前缀）。
- [11:00] 主题系统统一：`UiPalette` 从读 `dark.png` 像素改为读 JSON 主题 `assets/uifw/theme/default.json`（8 色，色值取自原 png），支持资源包覆盖换肤 + FALLBACK 兜底，新增 `reload()`；删除 `ThemeManager.themeU`、`ScrollBar.withTrackWidth/getTrackWidth`（死配置）、`GuiRenderTypes` 空 `Util.make`、`AbstractButtonGroup` 横纵死分支。
- [11:00] 独立发布：uifw 新增自身 `META-INF/neoforge.mods.toml`（modId `uifw`、独立版本 `uifw_version=1.0.0`、gradle.properties 新增 `uifw_mod_id/uifw_version`）、`archivesName=uifw-1.21.1`、processResources expand、maven 发布；主 JAR 合并 uifw 时 exclude 其 toml（避免与 generateModMetadata 冲突），主 toml displayName 改「uifw - UI Framework」。
- [11:30] 修复潜伏 bug：9 个 shader json 的 vertex/fragment 仍引用旧命名空间 `rtsbuilding:`（编译查不出、运行时加载失败）→ 改 `uifw:`；`GuiRenderTypes` render type 名 `rtsbuilding_gui_textured`→`uifw_gui_textured`。
- [11:30] lang 迁移：uifw 组件 color_picker 文案从主 mod key（`screen.rtsbuilding.color_picker.*`）迁到 uifw 自己 `screen.uifw.color_picker.*` + 新增 `assets/uifw/lang/{zh_cn,en_us}.json`；主 mod lang 删除无引用的 5 个 color_picker key。
- [11:30] 文档：AGENTS.md 模块结构补充 uifw 说明、lang 约定补充 `screen.uifw.*` 前缀与独立 lang 文件位置。
- [12:00] 验证：`:rtsbuilding-main:build` 通过（112 测试 0 失败、verifyAddonPackaging 5 addon 通过）；uifw 独立 jar `uifw-1.21.1-1.0.0.jar` 产物检查（shader json `uifw:` 引用正确、theme/lang/纹理齐备）；主 JAR 检查（单 mods.toml、uifw modId 声明、uifw 类合入、无 rtsbuilding_ui 残留）。
- [13:00] 修复 dev 运行 `fml.modloadingissue.javafml.dangling_entrypoint`（6 个 mod 全失效）：根因是 uifw 模块自带的 `META-INF/neoforge.mods.toml` 放在 `src/main/resources`，dev 类路径上与主 toml（generateModMetadata 生成）共存，两者都声明 `modId="uifw"` 导致 FML mod 容器初始化冲突。修复：toml 移到 `src/main/templates/META-INF`，改由 jar 任务注入（独立 jar 才有 toml，dev 类路径无 toml）；验证 uifw dev resources 无 toml / 独立 jar 有 toml / 主 jar 单 toml，主 mod build 通过。
- [14:00] uifw 主题系统重构（大工程）：① `UiPalette` 重写为 JSON 主题引擎——自动扫描所有 mod 的 `assets/<ns>/theme/uifw.json` 与 uifw 自带 `default.json`，按 `priority` 合并（后加载覆盖），新增 `get(key)` 按组件 key 取色、`reload()`、明暗双模式（`colors` 基线 + `light_overrides`）；② `ThemeManager` 收敛为明暗切换 + 通知，`getTextColor` 等委托 UiPalette；③ `default.json` 全组件 key 化（基础 8 色 + text/button/scroll/slider/toggle/input/section/popup/list/tooltip/picker/status/tab 等约 60 key，宿主 JSON 可覆盖任意 key）。
- [14:00] 组件硬编码色全迁移：uifw 组件（UiButton/ScrollBar/ScaleSlider/ToggleSwitch/CollapsibleSection/BasePopup/TextInputBox/NumericInputBox/HexInputComponent/ResetButton/ColorWheel/ColorPickerPanel/SettingsSection/WindowFrameRenderer/TextRenderer）+ 主 mod UI 面板（BuilderScreen/GridRenderer/BlueprintLibraryPanel/WorkflowRenderer/LogoMenuPopup/ModeSwitcher/RenderingSection/BindingRenderer/PageTabBar/ScreenBackgroundPanel/BlueprintSavePanel/ResumeWorkflowPanel）迁到 `UiPalette.get(key)`；世界渲染/坐标轴/状态灯/进度/绑定操作等业务语义色保留硬编码。
- [14:30] 事故与恢复（PowerShell 脚本字符级损坏）：`ReplaceIn` 函数因 PowerShell 数组参数行为导致 `$p[0]`/`$p[1]` 取字符串字符而非数组元素，8 个 uifw 文件（0→x、U→i）与 2 个主 mod 文件（RenderingSection i→n、BindingRenderer l→i）被字符替换破坏。uifw 5 个读过原文的文件重写恢复，3 个（HexInputComponent/ColorWheelComponent/ResetButton）用 CFR 反编译 `build/classes` 恢复；主 mod 2 个用 CFR 反编译恢复 + 重新迁移。验证全仓 build 通过、112 测试 0 失败、jar 产物（default.json 全 key、无 rtsbuilding 残留）。
- [15:00] 主题系统验证：uifw 独立 jar `uifw-1.21.1-1.0.0.jar` 含完整 default.json（约 60 key + light_overrides）；`:rtsbuilding-main:build` 全过（verifyAddonPackaging 5 addon、112 测试）。宿主 mod 换肤方式：在自身 `assets/<modid>/theme/uifw.json` 放同结构 JSON（`priority` 可调），任意 key 覆盖 uifw 默认主题。
- [15:30] 主 mod 主题文件：新增 `rtsbuilding-main/src/main/resources/assets/rtsbuilding/theme/uifw.json`（全 60 key 参考副本，含下划线分组注释，`priority: 100`），主 jar 验证含入。
- [16:00] 完整暗/亮两套主题：`PersonalizationSection`（齿轮→个性化）已有明暗切换开关（`ThemeManager.toggle()`），补全 `light_overrides` 为完整亮色主题（bg #F2F4F7 浅色系 + 深色文本，覆盖按钮/滚动条/滑块/开关/输入框/折叠/弹窗/列表/tooltip/进度条/取色器等全部 key）；uifw `default.json` 同步补全，任何用 uifw 的 mod 默认即有两套主题。切换开关直接可用，主 jar/uifw jar 均验证含亮暗两套配色。
- [16:30] 「宿主集成」分区无集成时自动隐藏：`IntegrationSection` 新增 `hasIntegrations()`；`GearMenuPanel` 的 totalSectionHeight/render/handleClick 三处仅在存在宿主集成（AE2/RS/BD/SB 任一注册）时参与布局，否则整块不显示（不再出现"无宿主集成"占位行）。
- [17:00] uifw 贴图全移除（矢量化，大工程）：删除 `assets/uifw/textures` 全部 6 张贴图（colorwheel/close_button/base_ui_1/2/6/dark）。① `ColorWheelComponent` 程序化生成 HSV 色轮（NativeImage 逐像素 + DynamicTexture 注册为动态纹理），读像素取色/HS↔UV 逻辑保留，`renderWheel` 改 `blit` 绘制；② `ColorPickerButton` 图标改 blit 共享色轮纹理缩略图；③ 新增 `CloseButton` 矢量关闭按钮（SDF 圆角背景 + Bresenham × 图标），替换 `UiButton`+close_button.png 贴图，`UiPanel`/`WindowFrameRenderer` 同步；④ `SpriteRenderer` 删除 uifw 专属九宫格贴图面板方法（drawNineSlicePanel/DragPanel/FloatingPanel + PANEL/DRAG/FLOATING 常量），保留通用 drawSprite/drawNineSlice/drawTiledGrid（主 mod 业务贴图仍用）；⑤ `WindowFrameRenderer` 删除贴图背景路径，纯 SDF；⑥ `dark.png` 已随 UiPalette JSON 化无引用。全仓 build 通过、112 测试 0 失败、uifw jar 0 贴图 0 png、主 jar 仅保留主 mod 业务贴图。
- [17:30] 主 mod 未引用贴图清理：全量扫描 `assets/rtsbuilding/textures` 41 个贴图的源码/模型引用，删除 uifw 拆分遗留的 6 个（close_button/dark/base_ui_1/2/6/colorwheel）+ 0 字节垃圾文件 `gui/We are Falcons！！！`，清空目录 gui/color。剩余 34 个贴图全部有引用（含被使用的 base_ui_5/7），复扫未引用 0。全仓 build 通过。
- [18:00] 调色盘与关闭按钮纯矢量化：① 新增 `colorwheel` SDF shader（fragment 内 HSV→RGB + 圆形裁剪 + 抗锯齿），`SdfRenderer.drawColorWheel` 矢量绘制，`RtsUiMod` 注册第 10 个 shader；② `ColorWheelComponent` 从"运行时生成位图 + 读像素取色"改为**纯数学取色**（鼠标→极坐标→HSV→RGB，复用 `ColorPickerPanel.ColorMath`），删除 NativeImage/DynamicTexture/readPixel/findNearestValidColor/共享纹理；③ `ColorPickerButton` 图标改 `drawColorWheel` 矢量绘制；④ `CloseButton` 的 × 从 Bresenham 像素线改为两条旋转的 SDF 圆角矩形。uifw jar 0 贴图、shader 31 文件，全仓 build 通过。
- [18:30] 新增 `CircleColorSwatch` 矢量圆形色块组件（`com.rtsbuilding.uifw.component`）：SDF 圆绘制，支持纯色填充与带描边（外圆描边色 + 内圆填充色）两种形态，颜色由调用方传入，无贴图。全仓 build 通过。
- [19:00] 圆形色块接入颜色显示：`ColorPickerPanel` 三处方形色块改 `CircleColorSwatch` 圆形——① `ColorBlockComponent` 内部改圆（主 mod `RenderingSection` 设置面板 5 处颜色行自动变圆）；② `ColorPreviewComponent` 预览从双色横条改为「初始色圆 + 当前色圆 + 值文本」（命中逻辑不变）；③ `SwatchSelectorComponent` 预设色块改圆 + 选中/未选中圆描边。全仓 build 通过。
- [19:30] lang 缺失 key 修复：补齐 `screen.rtsbuilding.settings.linked_storage_color`（设置面板"绑定容器线框颜色"标签）；全量校验代码引用的 lang key，修复 7 处——`resume.material_missing_item`（孤儿 key）改名为代码实际用的 `resume.missing_item`，补 `blueprints.status.{disabled,empty,too_many_blocks,parse_failed,queued,complete_partial}`（蓝图执行状态消息，含 %s 占位符）；确认动态 key（mode./left.shape.）全部覆盖。zh/en 231=231 一致，静态缺失清零，全仓 build 通过。
- [20:00] uifw 新增轻量布局包（借鉴 LDLib2 flexbox 思想，纯 Java 无依赖）：`com.rtsbuilding.uifw.layout`——`UiSize`（px/percent/auto 三态 + min/max 解析）、`UiBox`（子项约束：fixed/content/fill/percent + flex 权重 + min/max）、`UiRect`（布局结果）、`FlexLayout`（ROW/COLUMN + Justify[START/CENTER/END/SPACE_BETWEEN/SPACE_EVENLY] + Align[STRETCH/START/CENTER/END] + gap + flex 权重分配剩余空间）、`GridLayout`（固定列数均分 + gap + 可选居中 cell）。配置 uifw test（JUnit5），17 个布局单测覆盖固定/间隙/flex 权重/justify/column/百分比/交叉轴对齐/网格/尺寸 clamp，全过。全仓 build 通过。
- [20:30] 布局器落地 main（受益处改造 + 定为标准）：评估 17 个面板后确认——机械全改风险高（tooltip/滚动/命中坐标强耦合）且骨架面板非 flex 语义，采用"改真正受益处 + 定为新面板标准"。示范改造 `BlueprintLibraryPanel` 列表行：`computeRowRects` 用 `FlexLayout`（内容区 fill + 删除按钮 fixed 右对齐，gap 3），渲染与删除按钮命中检测复用同一布局（消除两处手写 delX 计算）；AGENTS.md 新增「UI 布局规范」——新面板/重构一律用布局包，现有稳定面板不强改。全仓 build + 129 测试通过。
- [21:00] UiPanel 子类布局器化：`BlueprintSavePanel` 内容区用 `computeLayout`（SaveLayout record）统一——按钮行用 `FlexLayout` 居中（保存/取消）、渲染与点击命中共用同一布局（消除 render/click 两处重复的 inputY/btnRowX 手写）。至此 UiPanel 子类已有 2 个（BlueprintLibraryPanel、BlueprintSavePanel）用布局器。全仓 build + 129 测试通过。
- [21:30] 其余 UiPanel 子类布局器化：`ResumeWorkflowPanel` 底部按钮行用 `FlexLayout` 居中（冲突时双按钮/单按钮）、材料行用 `FlexLayout`（图标 fixed + 进度条区 fill）；`GearMenuPanel` 五个分区用 `sectionRects`（ColumnLayout 垂直堆叠 + 滚动），渲染与点击命中共用同一布局。补 `java.util.List` import。全仓 build + 129 测试通过。
- [21:30] `InteractionPanel` 评估：为窗口协调器（注释"仅负责协调与窗口管理"），无自身内容排布；实际排布在 `PageTabBar`（带滚动 + 选中态的复杂标签栏，tab 位置规律递增），强行布局器化会破坏滚动裁剪，按 AGENTS.md「现有稳定面板不强改」保留，后续重构迁移。UiPanel 子类 5 个中 4 个已完成布局器化。
- [22:00] 调色盘面板（`ColorPickerPanel`）自动布局器化：① `computeWheelSectionLayout` 色轮区行用 `FlexLayout`（色轮区 fixed + 灰阶条 fixed，`Justify.START` + gap）；② 新增 `computeSliderRowLayout` 滑块行用 `FlexLayout`（标签 content + 滑块 fill），色相/饱和度两行滑块的位置与宽度由布局器计算，render 与滑块命中检测（hitTest）共用同一布局。全仓 build + 129 测试通过。
- [22:30] 调色盘默认尺寸 +30px：`ColorPickerPanel.getDefaultWidth` 改为 `computeContentWidth() + 32`（左右各 15 外边距）、`getDefaultHeight` 改为 `computeContentHeight() + getTitleBarHeight() + 38`（上 8 / 下 30）。全仓 build 通过。
- [23:00] 调色盘面板布局重构（统一布局器）：`ColorPickerPanel` 分散的 `computeWheelSectionLayout`/`computeSliderSectionLayout` 合并为 `computeContentLayout`（返回 `PickerLayout` 记录），整列用 `FlexLayout` 列布局堆叠 [色轮区/预览/hex/色块(可选)/色相滑块/饱和度滑块]，色轮区行内居中 + 滑块行内 [标签,滑块]，渲染/点击/拖拽/滚动全部复用同一布局（消除 render/click/scroll 三处重复 Y 坐标手算与拖拽宽度硬编码 `SLIDER_LABEL_W`）；滑块标签「色相/饱和度」迁到 lang（`screen.uifw.color_picker.hue/saturation`）。uifw 编译 + 17 布局测试通过。
- [23:40] 调色盘布局升级为「三区块卡片式」（用户选定方向）：内容区改为四卡片纵向排布——① 色轮卡片（色轮+灰阶条居中）② 滑块卡片（色相/饱和度两行）③ 预览+输入卡片（初始色圆/当前色圆与 hex 输入框同一行，`ColorPreviewComponent` 改为紧凑双圆模式）④ 色块卡片（可选，置底）；卡片用 `drawBorderedRoundedRect` 背景（border+bg），布局仍由 `computeContentLayout` 统一计算，渲染/点击/拖拽/滚动复用；`computeContentHeight/Width` 同步。uifw 编译 + 17 测试通过。
- [00:00] 滑块条目细节调整（用户要求）：色相/饱和度两行条目——① 行高 `SLIDER_ROW_GAP`(14)→`SLIDER_ROW_H`(20)，每个条目独立绘制圆角背景（新增主题 key `slider_row_bg`，暗 `#DD1A232E`/亮 `#F0F3F6`，uifw default.json 与主 mod `rtsbuilding/theme/uifw.json` 同步）；② `computeSliderTrack` 改左右半区布局——左半区文字靠左 +2px（`SLIDER_LABEL_LEFT_PAD`）、右半区轨道靠右 -2px（`SLIDER_TRACK_RIGHT_PAD`），文字与轨道分区绘制，渲染/命中/拖拽仍复用同一布局。uifw 编译 + 17 测试通过。
- [00:00] 滑块条目文字与轨道对齐：文字左边缘对齐下方初始色圆左边缘（`ColorPreviewComponent.initialSwatchLeft`），轨道左移相同偏移（`computeSliderTrack` 增加 shift 参数）；移除滑块卡片整层 `drawBorderedRoundedRect` 背景，仅保留条目 `slider_row_bg`。uifw 编译 + 17 测试通过。
- [00:00] 色轮颠倒修复：`colorwheel.fsh` 的 hue 计算 `atan(y,x)/2π+0.5` 与取色 `pickColor`（`atan2/2π+1.0 mod 1`）相差 180°，导致显示颜色与取色错位；shader 改为 `fract(atan(y,x)/2π+1.0)` 与取色公式对齐，渲染/取色/指示器三方坐标一致。uifw 编译 + 17 测试通过。
- [00:00] 颜色编码行细节（用户要求）：输入卡片高度与色相条目一致（`INPUT_CARD_H = SLIDER_ROW_H + 4`=24）；`HexInputComponent` 新增 `modeBtnRightOffset` 使 HEX/DEC 按钮左移对齐初始色圆左边缘（`ColorPreviewComponent.previewLeftPad()`），`MODE_GAP` 4→6（输入框与按钮间隔 6px）。uifw 编译 + 17 测试通过。
- [01:00] 设置面板滚动条与文字对齐（用户反馈）：分区卡片从 `cy + CONTENT_TOP_PAD` 绘制但滚动条轨道从 `cy` 开始，中心错位；`GearMenuPanel` 新增 `scrollBarTrackY/Len`（顶部内缩 CONTENT_TOP_PAD），render/click/drag 三处统一复用，轨道中心与文字区中心对齐。全仓编译 + 测试通过。
- [01:30] 设置面板滚动条中心对齐（二次修复，用户复测仍偏移）：撤回首轮轨道偏移方案，改为内容贴顶——`totalSectionHeight` 去掉 `CONTENT_TOP_PAD` 虚高、`scrolledCy = cy - CONTENT_TOP_PAD - scroll` 补偿分区卡片内边距使首个分区贴内容区顶部，轨道恢复覆盖整个内容窗口 `[cy, cy+ch]`；`ScrollBar` 新增可配置 `scrollBottomPad`（默认 6 保持其他面板不变）+ `withScrollBottomPad(0)` 使滚到底内容贴底。任意滚动位置内容中心=轨道中心=窗口中心。全仓编译 + 测试通过。
- [02:00] 修复「小窗口打开 Logo 菜单→放大窗口→点击 RTS 设置无反应」（用户反馈）：根因是窗口 resize 触发 `BuilderScreen.init()` 重建 `LogoMenuPopup`/`FileMenuPopup`，而 popup 动作是"一次性暂存"（`pendingOnGearMenuToggle` 等 init 时消费后置空），重建后新 popup 的 `onGearMenuToggle` 为 null，点击被静默跳过。修复：`TopBarPanel` 将 pending 字段改为持久 action 字段（`gearMenuToggleAction`/`openBlueprintLibraryAction`/`importBlueprintAction`），init 重建 popup 后重新绑定；附带修复 `BuilderScreen.init()` 每次向 `floatingWindowLayer` 重复添加面板（新增 `addFloatingWindowIfAbsent`）。全仓编译 + 测试通过。
- [02:30] 面板打开位置统一（用户要求，以设置面板为基准）：uifw `UiPanel` 新增通用居中方法 `positionCentered(topInset, bottomInset)`（水平居中 + 垂直居中 + 顶/底约束）；主 mod 各面板统一调用 `positionCentered(TOP_H+6, 8)`——`GearMenuPanel`/`InteractionPanel` 收敛（行为不变）、`ResumeWorkflowPanel` 从偏上 1/3 改居中、`BlueprintSavePanel` 补底部约束、`BlueprintLibraryPanel` 从固定左上角 (8,60) 改为尺寸自适应 + 居中；uifw `ColorPickerPanel` 收敛 `positionCentered(60, 8)`。全仓编译 + 测试通过。
- [03:00] 蓝图库删除按钮矢量化（用户要求，使用 uifw 删除 UI）：uifw 新增矢量垃圾桶图标 `SdfRenderer.drawTrashIcon`（盖子/提手/桶身/分隔线四段 SDF 圆角矩形组合）+ 新组件 `DeleteButton`（SDF 圆角背景 + 悬停渐变 + 垃圾桶图标，confirm 态红色背景 + 问号提示二次确认）；`BlueprintLibraryPanel` 删除按钮从文字 "x"/"?" 改为 `DeleteButton`（删除原 `DEL_BTN_W` 常量与手写圆角/文字绘制，渲染与命中检测共用 `deleteButton.hit`）。全仓编译 + 测试通过。
- [04:00] 修复「鼠标中键旋转 / Shift+右键平移摄像机无响应」（用户反馈，链路检查）：根因是 `BuilderScreen.mouseDragged` 经 `BuilderScreenScaleManager.scaleMouseEventQuad` 时把拖拽增量 `dragX/dragY` 也除以 RTS GUI 缩放系数（坐标需缩放，增量是屏幕像素位移不应缩放）。窗口 GUI 缩放 ≠ 2（小窗口常见 scale=1 → 系数 2）时拖拽量被缩小，`CameraInputLayer` 的 5px 拖拽阈值迟迟不满足 → 旋转/平移完全无响应。修复：`scaleMouseEventQuad` 只缩放坐标、保持增量原值，并补注释说明。全仓编译 + 测试通过。
- [04:30] 修复「GUI 缩放 ≠ 2 时画面/瞄准偏移」（用户定位到根因）：`BuilderScreenScaleManager.getRtsGuiScale()` 在 auto 模式错误返回 `mc.getWindow().getGuiScale()`，而 RTS 渲染始终以 `fixedRtsGuiScale`（默认 2.0）为虚拟坐标基准（`pose.scale(2/guiScale)`）。guiScale ≠ 2 时，所有依赖 `getRtsGuiScale()`/`getRtsVirtualWidth()` 的坐标计算（射线 CursorRaycaster、UI 命中 isMouseOverUiPanelApi、世界区域判断、InteractionTargetPass、CursorWrapHandler、漏斗检测）与渲染坐标系错位 → 瞄准/拾取偏移。修复：`getRtsGuiScale()` 始终返回 `fixedRtsGuiScale`（auto 即默认 2.0），同步更新 BuilderScreen.getRtsVirtualWidth/Height 过时注释。全仓编译 + 测试通过。
