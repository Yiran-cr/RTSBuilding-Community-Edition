# 2026-08-19

- [--:--] 记忆规范中文化：将 `AGENTS.md` 中剩余的英文段落（项目简介、构建/运行、模块结构、规范约定、禁止改动）全部翻译为简体中文。
- [--:--] 重写 `AGENTS.md` 为完整架构索引：全面扫描 9 个子模块，新增「模块结构总览（包级职责表）」「核心架构数据流（RTS 模式/远程建造/相机权威/能量/AE2）」「扩展点与打包机制」章节，让其他开发者能看懂每个包/核心类的职责。
- [19:35] 蓝图结构预览：参考 LDLib2 WorldSceneRenderer/DummyWorld 移植到 rtsbuilding——新增 `client/scene/RtsDummyLevel`（虚拟 Level 存方块）+ `RtsSceneRenderer`（VBO 缓存 + FBO 相机渲染 + 球坐标旋转/缩放）；新增 `BlueprintPreviewPanel`（蓝图库单击选中打开 3D 预览），`BlueprintLibraryPanel` 改为单击选中/双击重命名，`BuilderScreen` 注册预览面板并退出时释放 GPU 资源，lang 新增 6 个 preview key。
- [22:10] 蓝图预览面板定位对齐：`BlueprintPreviewPanel.computeDefaultPosition()` 由右侧锚定改为统一基准 `positionCentered(TOP_H + 6, 8)`（尺寸自适应 + 水平/垂直居中），并将「面板生成位置统一」规范写入 AGENTS.md「六、规范约定」。

- [22:16] 蓝图列表悬停遮挡修复：BlueprintLibraryPanel 行悬停判断（行高亮 + 打开/删除按钮显隐）合并 HoverSuppression.floatingWindow().isSuppressed()，被上层浮窗/常驻面板覆盖时不再穿透显示。

- [22:19] 蓝图预览 360° 无死角旋转：RtsSceneRenderer.rotate pitch 由 [5,85] 放开到 [-90,90]；新增 computeCameraUp 在视线接近垂直时改用水平 up 向量，避免 lookAt 万向节锁退化。

- [22:25] 修复蓝图预览顶/底部死角：otate 去掉 pitch 的 [-90,90] clamp 改为自由累加（模 360）；视图由 lookAt+up 改为矩阵 Rx(pitch)·Ry(-yaw)·T(-eye) 构造，删除 computeCameraUp，pitch 越过 ±90° 连续翻转不卡顿、画面不突变。

- [22:30] 蓝图预览拖拽旋转体验：参考 XYZ 轴调节器（AxisGizmoInputHandler）+ CursorWrapHandler——拖拽时隐藏系统光标（记录起始位置，释放时恢复并移回）；拖拽开始后不再校验鼠标位置（离开预览区仍持续旋转）；每帧把光标钳制回屏幕中心突破屏幕边缘，实现无限旋转。BlueprintPreviewPanel 关闭 / 退出 RTS 时强制恢复光标。

- [22:40] 蓝图预览拖拽旋转方向反转：BlueprintPreviewPanel.mouseDragged 旋转增量取反（向左拖 → 结构正向旋转，向上拖 → pitch 增）。

- [22:42] 修复蓝图预览拖拽闪烁：光标钳制回屏幕中心会产生 GLFW 合成移动事件，MC 把「跳回中心」的位移也当拖拽增量导致每帧正反抖动。改为 otateByCursorDelta 自读光标绝对位置计算增量，并在钳制后把计算基点重置为屏幕中心，只累计真实拖动位移。

- [23:07] XYZ 轴调节器光标逻辑反向移植：把蓝图预览的「隐藏光标+钳制中心+自读绝对位置增量」方案应用到 AxisGizmoInputHandler——mouseDragged 弃用 MC 增量改 otateByCursorDelta 差分，新增 queueCursorCenter/pplyPendingCursorCenter（AxisViewGizmo.render 阶段应用），消除合成移动事件抖动并支持无限旋转；AxisGizmoState 新增 lastDrag 基点与 pendingCenter 字段。

- [23:13] 修复蓝图预览/XYZ 轴调节器旋转抖动：差分基点此前在 mouseDragged 里提前重置为屏幕中心，但钳制在渲染末尾才生效，同一帧多次 GLFW 移动事件的后续事件仍按旧位置差分导致旋转量错乱。改为基点更新为<b>本次实际位置</b>，仅在 pplyPendingCursorCenter 真正执行 glfwSetCursorPos 时才重置为中心。

- [23:21] 修复预览/XYZ 旋转抖动与轴向偏移：弃用 GLFW_CURSOR_HIDDEN+钳制屏幕中心（钳制跳变产生 GLFW 合成事件，MC 派发额外坐标/拖拽污染其它轴），改用 FPS 标准 GLFW_CURSOR_DISABLED（光标锁定+隐藏、GLFW 只报相对位移）——BlueprintPreviewPanel/AxisGizmoInputHandler 自读差分即真实增量，无合成事件、无抖动、无轴向偏移，且天然无限旋转；删除钳制中心/pendingCursorCenter 相关逻辑。

- [23:24] 旋转手感优化：蓝图预览灵敏度 0.6→0.3（度/像素）、XYZ 轴调节器增量 ×0.5，两处均加 0.2px 亚像素死区过滤噪声，降低跳变感提升丝滑度。

- [02:20] XYZ/预览拖拽期间关闭世界 pass 与悬浮判定：BlueprintPreviewPanel 暴露 isPreviewDragging，BuilderScreen 新增 isBlueprintPreviewDragging/isAnyDragActive（gizmo||预览）；RenderPipeline.onRenderFrame 在 isAnyDragActive 时整体跳过全部世界内 pass，BuilderScreen.render 跳过 oxSelector.updateHoverFromScreen，三个既有 pass（Box/LineBrush/InteractionTarget）条件改为 isAnyDragActive，避免光标锁定后 pass/悬浮渲染在错误位置。

- [02:34] 蓝图「使用」放置功能：库面板行内新增「预览」「使用」按钮（4 按钮布局），预览按钮开结构预览面板（标题附蓝图名）；「使用」→ BuilderScreen 放置模式（准星锚点+幽灵线框预览 BlueprintPlacementPreviewPass），左键确认发 C2S PLACE_BLUEPRINT(42)（ActionType 末尾新增，协议测试同步更新）→ ServerActionHandler 反序列化 NBT 蓝图 → BlueprintContext + PipelineRegistry.execute(BLUEPRINT_BUILD) 服务端逐格建造；Esc 取消。lang 新增 5 个 key。

- [03:02] 蓝图库交互调整：单击条目仅选中高亮（不再打开预览），双击仍重命名，打开预览面板只走行内「预览」按钮。

- [03:04] 蓝图放置虚影改为真实方块模型：BlueprintPlacementPreviewPass 弃用纯蓝色线框填充，改按 BreakEffectPass 方案用 BlockRenderDispatcher.renderSingleBlock + RtsAlphaVertexConsumer 渲染每个蓝图方块目标位置的半透明真实模型虚影（alpha 0.45），非模型方块（流体）回退半透明色块。

- [03:07] 蓝图放置虚影外围加蓝色包围盒线框：BlueprintPlacementPreviewPass 在真实模型虚影外层用 CornerBracketRenderer.renderCornerBrackets 画锚点+声明尺寸的蓝色线框（深度层 + 穿透层），标识蓝图整体实际大小。

- [03:10] 蓝图放置支持 Ctrl 偏移：BuilderScreen 放置模式锚点更新时，按住 Ctrl（RtsKeyMappings.isPlaceOffsetDown）命中方块沿命中面方向偏移一格，与建造放置偏移一致。

- [03:12] 拖拽旋转时保留建造范围屏障：RenderPipeline.onRenderFrame 由整体跳过全部 pass 改为仅跳过除 BoundaryPass 外的世界内 pass（屏障墙不依赖鼠标坐标，持续标识建造范围）。

- [03:19] 悬浮文字/聊天框在下面板之上显示：RTS 覆盖式 Screen 下原版 HUD 不渲染，新增 RtsGuiOverlayMixin/RtsChatComponentMixin（实现 IRtsOverlayAccess/IRtsChatAccess 接口，shadow 暴露 actionbar 字段与 ChatComponent.allMessages），BuilderScreen.renderOverlayMessages 在下面板顶部（height-getDownSidebarHeight，随尺寸/缩放/拖拽高度动态）之上渲染 actionbar 悬浮消息与最近 5 条聊天消息。

- [03:27] 悬浮/聊天显示优化 + RTS 内嵌聊天输入：actionbar 悬浮文字改为以下面板 x 轴中心为基准；聊天消息补半透明背景条；新增 RTS 内嵌聊天输入框（按 T 打开普通聊天 / 按 / 打开命令模式，Enter 发送/命令、Esc 关闭，光标/剪贴板粘贴），不退出 RTS 模式；蓝图放置/库搜索编辑时屏蔽聊天热键。

- [03:33] 改用原版原生聊天：移除自绘输入框，RTS 下按 T// 打开原版 ChatScreen；RtsChatManager 记录 BuilderScreen，BuilderScreen.onClose 在聊天切换时跳过 RTS 清理，RtsChatScreenMixin 在 ChatScreen 关闭时恢复 BuilderScreen（聊天不退出 RTS），保留原版历史/补全；RTS 界面下聊天消息仍在下面板之上补渲染。

- [03:35] 修复 RtsChatScreenMixin 崩溃：ChatScreen 未重写 onClose（仅 emoved），注入目标由 onClose 改为 emoved；cancel 后 setScreen 恢复 BuilderScreen，二次进入 removed 时标志已清不拦截避免递归。

- [03:38] 聊天输入改为 RTS 界面内嵌（不切 screen）：移除 ChatScreen 切换方案（删 RtsChatScreenMixin/RtsChatManager，onClose 恢复原样）；恢复 BuilderScreen 内嵌输入框（T// 打开、原版风格深色输入条+前缀+光标、↑↓ 聊天历史、Esc 关闭、Enter 发送/命令），聊天消息与输入框均在下面板之上渲染。

- [03:46] 聊天改为原版组件内嵌：BuilderScreen 用原版 EditBox 作输入框 + 原版 ChatComponent.render 作聊天框，直接渲染在 RTS 界面内下面板之上（不切换 screen、不退出 RTS）；RtsChatComponentMixin 用 @Redirect 把 GuiGraphics.guiHeight() 重定向到 BuilderScreen 虚拟高减去下面板偏移，RtsChatManager 记录激活状态与偏移；↑↓ 历史/Enter 发送/Esc 关闭，鼠标点击转发输入框定位。

- [03:48] 修复 RtsChatComponentMixin @Redirect 崩溃：handler 签名需含目标方法全部参数，改为 (GuiGraphics receiver, GuiGraphics g, int tickCount, int mouseX, int mouseY, boolean focused)，非聊天激活时回退调用 guiGraphics.guiHeight()。

- [12:39] 移除聊天文本相关逻辑：删除 RtsChatManager/IRtsChatAccess/RtsChatComponentMixin 及 mixin 注册；BuilderScreen 移除聊天输入框（EditBox/chatInputOpen/历史/发送等全部方法）与聊天消息显示，keyPressed/charTyped/mouseClicked/onClose 恢复原样；BlueprintLibraryPanel.isEditingText 移除；保留 actionbar 悬浮消息（IRtsOverlayAccess/RtsGuiOverlayMixin）在下面板之上显示。
