# 2026-08-15

- [23:04] 玩家实体环绕模式移动修复：新增 LocalPlayerStepAiMixin（mixin LocalPlayer.aiStep，input.tick 后以 GLFW 物理键覆写 player.input 恢复 WASD/跳跃/疾跑，并按相机 localYaw 对齐玩家朝向使 W=屏幕上方）+ MinecraftTickMixin（mixin Minecraft.tick，玩家环绕模式下调用 handleKeybinds 恢复原版键位动作）；CameraState 新增 getOrbitAngle()。已注册进 rtsbuilding.mixins.json，编译+测试通过。
- [23:20] 玩家环绕镜头自动回正（第三人称镜头回正）：拖拽旋转松开后镜头 500ms 缓动转回玩家背后（target=atan2(sin(yaw),-cos(yaw))）。改动 CameraState（新增 playerOrbitAutoReturn/ReturnTarget/From/StartMs 状态）、PlayerOrbitCameraMode（startAutoReturn/advanceAutoReturn 插值，processInput 用户输入时取消）、CameraModule（startPlayerOrbitAutoReturn/cancelPlayerOrbitAutoReturn，queuePanDrag/queueRotateDrag/snapViewToAxis 取消回正）、CameraInputLayer（onMouseReleased 旋转拖拽结束触发）、CameraModeController（disable/clear 清理状态）、LocalPlayerStepAiMixin（玩家移动时取消回正）。编译+测试通过。
- [23:55] 放置/破坏动画改为 BuildingGadgets2 风格（替换原线框天降/碎块上飘）：RenderPipeline 新增 BLOCK_ANIMATION RenderType（BLOCK 顶点格式+光照+半透明）+ blockBuf + blockSource（MultiBufferSource），BufferAllocator 加 blockSource 字段；PlaceAnimationPass 改渲染真实方块模型从中心 0→1 缩放生长（easeOut+淡入）；BreakEffectPass 改真实方块模型 1→0 缩小 + 4 个带纹理碎块粒子向外飘散上升；新增 util/RtsAlphaVertexConsumer（透明度包装，参考 DireVertexConsumer）；流体/非 MODEL 方块回退半透明色块。编译+测试通过。
- [00:30] 放置动画"触发与结束"借鉴 BuildingGadgets2（动画结束才落位）：新增服务端延迟落位引擎 RtsBlockAnimationCommitter（schedulePlace 先发动画包、动画周期 12 ticks 后执行 onCommit 真正 setBlock，挂载 ServerTickEvent.Post，含玩家下线保护 isPlayerStillOnline）；RtsPlacementQuickBuild/BlueprintTickPipe/HistoryExecutor.restoreBlocks 改为延迟落位；客户端 RtsEffectStateTracker.registerPlace 改为动画包到达即播（不再等状态就绪），PlaceAnimationPass 时长对齐 600ms；清理 RtsEffectStateTracker 失效的 PENDING_PLACE 死代码。破坏路径保持立即移除（客户端绑定 AIR 变化启动 shrink，视觉与 BuildingGadgets2 等价）。编译+测试通过。
- [00:55] 放置虚影对齐原版 BuildingGadgets2（全程不透明纯缩放、无透明度）：RenderPipeline 新增不透明 RenderType BLOCK_ANIMATION_OPAQUE（不启用混合+写深度，对应原版 cutout）+ blockOpaqueBuf/blockOpaqueSource，BufferAllocator 加 blockOpaqueSource 字段；PlaceAnimationPass 去除 START_ALPHA 淡入与淡出阶段，grow 600ms 不透明缩放（与 12 ticks 落位对齐），移除 RtsAlphaVertexConsumer 使用；破坏动画保持半透明。编译+测试通过。
- [01:20] 实现 BuildingGadgets2 retryList 支撑依赖重试：RtsBlockAnimationCommitter.schedulePlace 改为 (BooleanSupplier attempt, Runnable onGiveUp) 签名，attempt 返回 false 表示 canSurvive 失败请求重试，引擎延迟 RETRY_TICKS=4 重试（最多 MAX_RETRIES=1 次，重试不发动画包），超限调用 onGiveUp 退款；RtsPlacementQuickBuild/BlueprintTickPipe 落位前加 state.canSurvive 检查并接入重试/退款，HistoryExecutor 撤销恢复对齐 BG2 undo 不启用重试。编译+测试通过。
- [01:50] 放置/破坏动画服务端权威化：S2CRtsPlaceAnimationPayload/S2CRtsBreakAnimationPayload 新增 durationTicks 字段（服务端权威动画时长）；RtsPlacementSound.playRemotePlacedBlockAnimation 与 RtsMiningNetworkHelper.sendBreakAnimation 发送时携带时长（committer 统一 PLACE/BREAK_ANIMATION_TICKS=12）；GhostRingBuffer 每条目存储 durationMs（schedule/add/forEach/prune 签名更新，prune 按各自时长清理）；RtsEffectStateTracker 传递 durationMs（PendingBreak 携带）；RtsClientNetworkHandlers 将 durationTicks×50 传入；PlaceAnimationPass/BreakEffectPass 不再硬编码时长、按条目 durationMs 播放与清理；GhostRingBufferTest 更新签名并新增按条目时长清理/刷新时长测试。编译+测试通过。
- [02:20] 蓝图多方块占位物品材料去重（完全参考 BG2 掉落物导向）：RtsBlueprint 新增 explicitMaterialItemIds（仅显式材质，不含 asItem 兜底）；BlockPlacementPlanner.compute 加 ServerLevel 参数，materialItems 改为 silk touch LootTable 计算（门/床/双层植物上格无掉落 → 不扣费，异常回退 asItem，AE2 线缆保留 NBT 显式材质）；BlueprintExecutePipe/BlueprintPersistence 传 level；BlueprintTickPipe 修复：hasAllMaterialsForPlan 对 items 空非流体返回 true、canStillPlace 去掉 canSurvive 预检（交给落位重试）、attemptPlaceOne items 空非流体正常放置（不再 UNSUPPORTED）——修复门 UPPER 被 BLOCKED/挂起拦截、导致蓝图放门只有下半段或扣双份材料的问题。编译+测试通过。
