# 2026-08-18

- [00:47] 精灵图精度绘制重构：`SpriteRenderer` 提取私有 `emitQuad`（UV 归一化 + 4 顶点 QUAD 唯一实现），`drawSprite`×3 / `drawSpriteImmediate` / `drawNineSliceRaw` / `drawTiledGrid` / `drawTiledRowRange` 全部委托，消除 6+ 处重复的 UV 归一化代码（287→249 行）；新增 `drawSprite(..., alpha)` 重载，`GridSlotRenderer.drawOverlaySprite` 重复实现删除并改用 `SpriteRenderer.drawSprite`（含 alpha）。
- [00:47] 验证：`:rtsbuilding-main:compileJava` 与 `:rtsbuilding-main:test` 全过（112 测试 0 失败）；公共 API 签名保持兼容，调用点零改动。
- [00:55] 修复 blur 不生效：根因是 `TextureStateShard` 每次绘制强制 `setFilter(blur,mipmap)`，uifw `FilterMode` 覆盖 mcmeta；`ShapeButtonGroup` 构造 `TextureInfo` 的 `FilterMode.PIXEL` 改为 `NORMAL`（映射 linear=true），形状图标 GL_LINEAR 平滑生效，与 `right_button/*.png.mcmeta` 的 `blur:true` 语义一致。编译+112 测试通过。
- [01:30] 图标 mipmap 平滑（深入修复）：调查确认 SimpleTexture 永不生成 mipmap（`NativeImage.upload` mipmap 参数恒 false、mcmeta `mipmaps` 字段不读取），512→24px 21 倍缩放下 GL_LINEAR 无 mipmap 严重混叠。新增 uifw `MipmapTexture`（加载时 `MipmapGenerator.generateMipLevels` 生成完整 mip 链 + `GL_LINEAR_MIPMAP_LINEAR`）；`ShapeButtonGroup` filterMode 改 `HQ` + 新增 `registerMipmapTextures()`（TextureManager 注册 7 张图标）；主 mod 新增 `RtsMipmapTextures`（`RegisterClientReloadListenersEvent` 挂 reload 监听，资源重载后重新注册），`RtsClientBootstrap.onClientSetup` 启动预注册。`:rtsbuilding-main:build` 全过（112 测试、verifyAddonPackaging 5 addon），新类已合入主 jar。
- [02:00] 修复 mipmap 黑块：`MipmapTexture` 的 mip 层数误用 `ceillog2(max(宽,高))`（1024x512→10），`TextureUtil.prepareImage` 的 mip 循环 `width>>i` 无 `max(1,)` 保护，分配 level10=(1,0) 触发 GL_INVALID_VALUE 致纹理不完整渲染为黑块；改 `ceillog2(min(宽,高))`（→9，末级 2x1 合法），并加 levels<=0 时退回非 mipmap 过滤的防御。编译+112 测试通过。
- [02:10] 删除 7 张形状图标的失效 mcmeta（`right_button/*.png.mcmeta`，blur:true 已完全无效——`MipmapTexture` 加载只读 PNG 不读 mcmeta），jar 验证无残留；保留 `rtsbuilding_technologized` 方块纹理 mcmeta（vanilla 管线，不受影响）。`:rtsbuilding-main:build` 全过。
- [02:40] 左面板 `button/` 8 张图标接入 mipmap 平滑：`AbstractButtonGroup` 新增 `FilterMode` 构造重载（默认 PIXEL 保持兼容）；`UltimineButtonGroup`/`SelectButtonGroup`/`BuildDestroyButtonGroup`/`ActionButtonGroup` 改用 `HQ`（纹理常量改 public）；注册集中到 `RtsMipmapTextures.registerAll()`（15 张 = 7 right_button + 8 button），`ShapeButtonGroup.registerMipmapTextures` 删除并入统一入口，`RtsClientBootstrap` 改调 `registerAll`；`UtilityButtonGroup` 的 `null` 参数显式 `(ResourceLocation)` 消除重载歧义。`:rtsbuilding-main:build` 全过（112 测试、verifyAddonPackaging 5 addon）。
- [03:10] 顶栏 `gui/top/` 5 张接入 mipmap 平滑：`CameraModeGroup`（free/surround_mode）、`UtilityButtonGroup`（chunk_display）构造改 `HQ`；`TopBarPanel` LOGO 与 `LogoMenuPopup` setting 的 `TextureInfo` 改 `HQ`；`RtsMipmapTextures.registerAll` 追加 5 张（logo/setting/free_mode/surround_mode/chunk_display，共 20 张）。`:rtsbuilding-main:build` 全过。
- [03:15] 记忆沉淀：AGENTS.md Conventions 新增「大尺寸贴图必须模糊化（mipmap）」规范（FilterMode.HQ + `RtsMipmapTextures.registerAll` 注册 + 2 的幂约束；禁写 blur mcmeta；记录已迁移目录）。
- [03:30] 蓝图文件面板补 UI 动画：`BlueprintLibraryPanel` 手写渲染部分接入 uifw 动画系统——搜索框焦点/悬停（`searchFocusAnim`+`searchHoverAnim`，同 GridRenderer）、列表行悬停（`Map<Path,AnimFloat> rowHoverAnims` + ColorAnimation.lerpRGB，同 WorkflowRenderer）、重命名编辑框焦点（`renameFocusAnim`）；`refreshFiles`/`onClose` 清理动画缓存并 snapTo 复位。编译+112 测试通过。
- [03:45] 删除按钮图标改文字：uifw `DeleteButton` 由垃圾桶图标改为本地化文字按钮（中文「删除」/英文「Del」，确认态「删除?」/「Del?」）；宽度按语言渲染宽度自适应（普通/确认态取较宽者，宽度不跳动），新增 `DeleteButton.width()`，`BlueprintLibraryPanel` 行布局改用它；文案走 uifw 自己的 lang（`button.uifw.delete`/`button.uifw.delete_confirm`）。编译+112 测试通过。
- [04:00] 蓝图文件面板新增行内「打开文件」按钮：每个条目行排布改为「[内容区 fill, 打开文件按钮 fixed, 删除按钮 fixed]」（`BlueprintLibraryPanel.computeRowRects`），按钮本地化文字（`button.rtsbuilding.blueprint.open_file`，中文「打开文件」/英文「Open File」）+ hover 动画（`Map<Path,AnimFloat> openFileBtnHovers`），宽度按文字自适应；点击 `openFileLocation(file)` 打开文件所在文件夹——Windows `explorer /select,` 定位选中文件、macOS `open -R`、其余 `Desktop.open(父目录)`，失败走 statusMessage（`message.rtsbuilding.blueprint.open_file_failed`）；渲染与命中复用同一行布局。编译+112 测试通过。
- [04:15] 修复重命名蓝图"未输入却改写"：`BlueprintLibraryPanel.commitRename` 无条件调用 `BlueprintLocalStore.rename`，其内部 `sanitizeFileName` 会把空格等合法字符改写下划线（如 `my blueprint.nbt`→`my_blueprint.nbt`），导致进入编辑态什么都不输入直接提交也被莫名改名；现改为输入与原文件名（不含扩展名）一致时直接退出编辑态、不产生写入。编译+112 测试通过。
- [04:40] 新增蓝图导入面板 `BlueprintImportPanel`（网页式「文件转换」上传区样式）：内容区中央虚线圆角框 + 大加号图标（uifw `SdfRenderer.drawPlusIcon` 新增，横条+竖条圆角十字）+ 提示文字，hover 渐变；点击弹出系统文件对话框，经 `BuilderScreen.importBlueprintFile()`（重构为返回保存 Path、向上抛异常）转换导入并刷新蓝图文件面板，结果在面板内提示；顶栏「文件→导入」菜单改为打开此面板，`BuilderScreen` 注册面板实例。lang 新增 `screen.rtsbuilding.blueprint.import_panel.*` 5 个 key。编译+112 测试通过。
- [05:00] 文件选择对话框绕过 headless：参考 `E:\rtsbuilding` 项目方案，新增 `TinyFileDialogSupport`（LWJGL 内置 `TinyFileDialogs` 原生对话框能力探测，不经 AWT、不受 `java.awt.headless` 限制）；`BuilderScreen.chooseBlueprintFile` 改为 TinyFD 优先（`tinyfd_openFileDialog` + 5 种扩展名过滤器）、AWT FileDialog 兜底。编译+112 测试通过。
- [05:20] 蓝图导入面板加号改贴图：`BlueprintImportPanel` 矢量 `drawPlusIcon` 替换为 `textures/gui/blueprint/update.png`（1024x512，暗亮两半区 HORIZONTAL_PAIR，FilterMode.HQ），经 `SpriteRenderer.drawSprite` + `getThemeOffset` 按主题取半区，悬停透明度随动画过渡；`RtsMipmapTextures.registerAll` 追加注册该贴图（MipmapTexture，满足大图 mipmap 平滑规范）。编译+112 测试通过。
- [05:40] 蓝图导入面板交互调整：上传区改为仅展示（悬浮只做边框颜色过渡、去掉背景填充高亮、加号不随悬停变化）；新增面板底部居中「选择文件」按钮（`button.rtsbuilding.blueprint.choose_file`，文字自适应宽度 + hover 渐变），只有点击按钮才打开文件对话框，上传区不再响应点击；布局 record `ImportLayout`（上传区/按钮/状态行），渲染与命中共用；lang 的 `import_panel.click` 改为描述性 `import_panel.hint`。编译+112 测试通过。
- [06:00] 蓝图导入面板按钮移入上传区：`BlueprintImportPanel.computeLayout` 改为上传区占满内容区（上下留边距），「选择文件」按钮放在圆角框内部底部居中（`BTN_INNER_MARGIN`），状态提示位于按钮上方，仍渲染与命中共用同一布局。编译+112 测试通过。
- [06:20] 蓝图拖放导入（网页式）：确认 1.21.1 文件拖放 API 为 `Screen.onFilesDrop(List<Path>)`（GLFW drop 回调由 `MouseHandler` 调用，非 `filesDragged`）；`BuilderScreen` 覆写 `onFilesDrop` 并在导入面板打开时转发；`BlueprintImportPanel.onFilesDropped` 从拖入路径选出受支持扩展名文件，经新抽出的 `BuilderScreen.importBlueprintFromPath`（弹框与拖放共用转换+刷新核心）导入；lang 更新 `import_panel.hint`（拖入或点按钮）并新增 `import_panel.drop_invalid`。编译+112 测试通过。
- [06:45] 蓝图导入异步化 + 阶段进度条：`BlueprintLocalStore.importFile` 新增 `ImportStage` 枚举（READING/PARSING/WRITING）与带 `Consumer<ImportStage>` 回调的重载；`BlueprintImportPanel.startAsyncImport` 用 `CompletableFuture.supplyAsync` 后台导入（大文件不再卡渲染线程），阶段切换更新 `AtomicReference<ImportStage>`，进度条 `AnimFloat` 平滑过渡（0.15/0.5/0.88→1.0），完成/失败 `mc::execute` 切回主线程刷新蓝图面板并提示；按钮与拖放共用异步入口；`BuilderScreen.importBlueprintFile/importBlueprintFromPath` 移除、`chooseBlueprintFile` 改 public。lang 新增 `import_panel.stage.*` 3 个 key。编译+112 测试通过。
- [07:00] 修复导入进度 NPE 崩溃：`renderContent` 在 `importing=true` 但后台任务尚未回调首个阶段（`importStage` 为 null）时，`stageKey(null)` 对 switch 表达式触发 NPE（latest.log 崩溃：BlueprintImportPanel.java:255）。`stageKey` 增加 null 分支显示「正在准备导入…」；lang 新增 `import_panel.stage.preparing`。编译+112 测试通过。
- [07:10] 导入结果文字移到圆角框下方：`BlueprintImportPanel.computeLayout` 底部预留状态区，上传区高度相应收缩，`ImportLayout` 增加 `progressY`（框内按钮上方，进度条/阶段文字）与 `statusY`（圆角框下方，成功/失败结果）；新增 `STATUS_BOTTOM_MARGIN`。编译+112 测试通过。
- [07:30] 拖放导入限定上传区范围：`BlueprintImportPanel` 新增 `isInsideDropZone`（虚拟坐标命中上传区圆角框）；`BuilderScreen.onFilesDrop` 读取 `mouseHandler.xpos()/ypos()` 并经 `scaleManager.scaleMouseEvent` 换算到 RTS 虚拟坐标，只有落点落在圆角框内才触发 `onFilesDropped`，拖到面板其他区域不导入。编译+112 测试通过。
- [07:45] 修复拖放不触发导入：drop 回调阶段 RTS 缩放坐标系不稳定（`screen.width` 处于临时改写窗口期、与面板 bounds 基准不一致），坐标判断失败。改为 `onFilesDrop` 仅缓存路径（`pendingDropPaths`），`BuilderScreen.render` 开头在虚拟坐标系稳定时调用 `handlePendingFileDrop`（gui scaled→虚拟坐标换算，与渲染/命中同基准）判定落点后触发导入。编译+112 测试通过。
- [08:00] 拖放时悬浮动画：文件拖放期间 Windows OLE 接管鼠标消息，GLFW/MC 光标位置冻结（无 WM_MOUSEMOVE），hover 动画失效。`BlueprintImportPanel.liveCursorVirtual` 用 AWT `MouseInfo`（底层 GetCursorPos，拖拽光标始终跟随鼠标）获取真实屏幕光标并换算为 RTS 虚拟坐标（gui×虚拟宽÷gui 宽，与渲染同基准）；`renderContent` 的边框/按钮 hover 与 `BuilderScreen.handlePendingFileDrop` 落点判定均改用实时坐标，失败回退原坐标。编译+112 测试通过。
- [08:15] 多蓝图文件同时导入：`BuilderScreen.chooseBlueprintFile` → `chooseBlueprintFiles` 返回 `List<Path>`（tinyfd 多选 `allowMultipleSelect=true`，`|` 分隔解析，AWT 兜底单选）；`BlueprintImportPanel` 新增 `importIndex`/`importTotal` 驱动多文件进度（`(idx+阶段比例)/总数`），`startAsyncImport(List<Path>)` 后台逐个导入并统计成功/失败（`ImportOutcome`），拖放 `importableFiles` 收集全部受支持文件；结果提示区分单文件/多文件/部分失败（`import_panel.success_multi`/`partial`），阶段文字附加 `（%d/%d）`。编译+112 测试通过。
- [08:30] 拖放悬浮动画改用原生光标：AWT `MouseInfo` 在 MC 环境可能因 headless/AWT 未初始化抛异常回退到冻结坐标，hover 仍不触发。`BlueprintImportPanel.liveCursorVirtual` 改为 Windows 上用 LWJGL 内置 `org.lwjgl.system.windows.User32.GetCursorPos`（不依赖 AWT、OLE 拖放期间拖拽光标跟随鼠标仍可取真实位置），提取 `toVirtual` 统一换算屏幕物理坐标→RTS 虚拟坐标；其他平台回退 AWT MouseInfo。编译+112 测试通过。
- [08:40] 修复拖放导入无反应：`handlePendingFileDrop` 在外层 render（`this.width` 为 gui scaled 宽）调用，`toVirtual` 误用 `screen.getUiWidth()`（实为 guiW）当虚拟宽，算出 gui scaled 坐标与面板 bounds（虚拟坐标）错位导致落点命中失败。新增 `isVirtualCoordinateLayer`，仅在固定缩放渲染层内或 RTS 缩放==原版 GUI 缩放时处理拖放 pending，保证坐标与面板同基准。编译+112 测试通过。
- [08:55] 导入成功后快捷打开蓝图列表：`BlueprintImportPanel` 成功提示（`!statusIsError`）时在圆角框下方状态区显示「打开蓝图列表」按钮（`button.rtsbuilding.blueprint.open_library`，文字自适应 + hover 动画，`openLibraryHover`）；点击打开 `BuilderScreen.getBlueprintLibraryPanel().open()` 并关闭导入面板；抽公共 `renderTextButton`，`ImportLayout` 增加 `openLibraryBtn`（渲染与命中共用）。编译+112 测试通过。
- [09:05] 删除确认态闪烁提醒：uifw `DeleteButton` 新增 `flashAnim`（400ms EASE_OUT_QUAD）与 `triggerFlash()`，确认态播放时背景透明度随 `|sin(4πt)|` 振荡 2 次亮暗提醒；`BlueprintLibraryPanel` 首次点击删除进入确认态（`pendingDelete=file`）时调用 `triggerFlash`。编译+112 测试通过。
- [09:15] 删除确认闪烁放慢：`DeleteButton.flashAnim` 时长 400→800ms，波形由 `|sin(4πt)|`（4 次亮暗）改为余弦 `0.5+0.5cos(2πt)`（一次明-暗-明过渡，结尾平滑恢复全亮）。编译+112 测试通过。
- [09:25] 删除确认闪烁改「明-更亮-明」：`DeleteButton` 闪烁改为背景向 `ColorAnimation.scale(base,1.5f)`（1.5 倍亮色）按 `sin(πt)` 脉冲过渡一次后恢复常亮（替代透明度变暗方案），时长保持 800ms。编译+112 测试通过。
- [09:40] 下面板两嵌层空状态归入 lang：右侧物品网格 `GridRenderer.renderEmptyHint` 硬编码 `"No storage"` → `screen.rtsbuilding.rightdown.no_storage`（未绑定容器）；左侧绑定 `BindingRenderer` 硬编码 `"No linked"` → `screen.rtsbuilding.binding.no_linked`（未绑定任何容器），中英文同步。编译+112 测试通过。
