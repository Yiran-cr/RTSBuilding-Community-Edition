# 2026-08-16

- [14:00] 蓝图模式框选生成蓝图：新增 `TextInputBox`（通用文本输入框组件）、`BlueprintSavePanel`（蓝图保存对话框，回车/按钮确认）、`BlueprintLocalStore`（捕获框选区域并保存本地 .nbt 蓝图，含非法字符清洗与同名去重）。
- [14:00] 蓝图模式框选完成按回车触发保存：`BuilderScreenEventRouter.handleShortcut` 增加 Enter 分支（蓝图模式 + 框选 COMPLETE + 非点击模式）；`BuilderScreen` 注册并暴露 `BlueprintSavePanel`。
- [14:00] `BlueprintWriters.capture` 增加 `skipFloor` 重载（框选完整捕获，不再跳过地板层）；lang 新增保存对话框与蓝图保存消息 key（zh_cn/en_us 同步）。
- [14:30] 新增右键框内触发：`BuilderScreen.tryOpenBlueprintSave()` 统一触发逻辑（蓝图模式+框选 COMPLETE+非点击+Config 开关），`BuilderScreenEventRouter` P_SELECTION 层右键框内点击后调用；回车分支复用同一方法。
- [15:00] 顶栏「文件」菜单：新增 `FileMenuPopup`（菜单项：蓝图文件/导入），`TopBarPanel` 文件按钮切换菜单并接线回调。
- [15:00] 蓝图文件管理面板：新增 `BlueprintLibraryPanel`（RtsPanel 浮窗，列出本地 .nbt 蓝图，滚动/二次点击删除/刷新），`BuilderScreen` 注册并接线「文件→蓝图文件」。
- [15:00] 蓝图导入：`BlueprintLocalStore` 新增 `listBlueprints/delete/importFile`；`BuilderScreen.importBlueprintFile` 用 AWT FileDialog 选外部蓝图 → `BlueprintReaders` 自动转换（Schem/Litematic/Building Gadgets/NBT）→ 存为本地 .nbt；lang 新增文件菜单/导入/删除 key。
- [15:30] 修复「文件」菜单无反应：`setOnOpenBlueprintLibrary`/`setOnImportBlueprint` 在 `filePopup` 创建前（BuilderScreen 构造早于 TopBarPanel.init）被调用时静默丢弃，新增 pending 回调暂存机制（与 logoPopup 的 pendingOnGearMenuToggle 一致），init 时应用。
- [16:00] 修复「导入」无反应：AWT FileDialog 改为在 `EventQueue.invokeAndWait`（EDT）中创建显示，避免渲染线程直接操作 AWT 组件导致对话框无法弹出；增加 headless 环境检查与失败提示。
- [16:30] 蓝图文件面板新增搜索框：顶部复用 `TextInputBox` 输入，实时过滤（输入即刷新）；匹配逻辑含文件名包含 + 拼音搜索（复用 `RtsPinyinSearch`，支持中文/全拼/首字母模糊匹配）；新增 `allFiles`（全量）与 `files`（过滤后）双列表、无匹配提示、lang key（zh/en）。
- [17:00] 重写蓝图文件面板搜索框为网格界面同款逻辑（对齐 `plugin.grid`）：弃用 `TextInputBox` 回车提交模式，改为主面板自身维护 `searchFocused`/`searchBuffer`/`searchCursorPos`，输入即实时过滤，Enter/Esc/点击外部失焦，渲染与光标闪烁同 `GridRenderer`，拼音匹配保留。
- [17:30] 修复蓝图文件面板搜索框高度：`SEARCH_H` 12→18（对齐 `GridRenderer.SEARCH_INPUT_H`）、`SEARCH_PAD` 改作文本内边距 4、搜索框左右外边距 8px，点击区域与渲染同步。
- [17:45] 蓝图文件面板搜索框与条目列表之间新增水平分界线（`BlueprintLibraryPanel.renderContent`）。
- [17:50] 蓝图文件面板条目列表添加常驻行背景：奇偶行交替底色 + 悬停高亮（`BlueprintLibraryPanel.renderContent`）。
- [18:00] 蓝图条目重命名：点击条目名字进入重命名编辑态（`BlueprintLibraryPanel` 增加 rename 状态机，Enter 提交/Esc 取消，与搜索框互斥）；`BlueprintLocalStore.rename` 用 `Files.move` 改名并自动同名去重；lang 新增重命名成功/失败消息。
- [18:30] 架构评审（非代码改动，仅新增文档）：全仓 7 模块/6.5 万行走读，产出 `docs/architecture-review.md`（模块依赖、解耦三板斧、单 JAR 多 mod、数据流、P0-P4 问题清单与路线图）；核实 RS 集成 `hasPermission` 恒 false 的只读缺陷。
- [19:00] docs SPA 接入架构评审：新增 `docs/app/src/ArchReview.vue`（glob 内联 `architecture-review.md` + marked 渲染 + 头部统计条），`App.vue` 增加「架构评审」Tab，`style.css` 补充 `.md-body` Markdown 样式；`npm run build` 通过并确认正文内联进产物。
- [19:30] 新增 `docs/architecture-optimization.md`（RTS-ARCH-2026.08）：分六阶段的架构整改方案（协议显式 id 化、RtsIntegration SPI 统一 addon、common 边界净化、服务端状态机化、测试与 CI 基建、打包自动化），含风险取舍与实施顺序。
- [19:30] `ArchReview.vue` 改造为双文档切换：glob 改为 `architecture-*.md`，新增「架构评审/架构优化方案」filter-chip 切换，统计条按文档分别提取；`npm run build` 通过。
- [20:00] 修复 docs SPA 架构评审页空白：`ArchReview.vue` 模板引用 `current.*` 但 setup 中 computed 名为 `active`，`current` 未定义导致渲染抛 TypeError（点击 Tab/切换按钮无内容）；改名对齐，并修正两个统计正则（review 的 `**P0-1` 加粗段、optimization 反引号开头行）。
- [21:00] 新增架构优化任务记忆 `docs/architecture-optimization-todo.md`（临时记忆，含 P0-P4 修复清单与确认机制），按"每解决一步更新确认"推进阶段一。
- [21:00] 阶段一·1.1 ActionType 显式值化：`core/network/ActionType.java` 加 `int id`（id==历史 ordinal，零协议破坏）+ `fromId()` switch 映射；`C2SAction` 改按 id 编解码；新增 `ActionTypeProtocolTest`（7 测试全通过：id 唯一/连续/与 ordinal 一致/fromId 全覆盖/未知返 null/占位符 @Deprecated）。
- [21:30] 阶段一·1.2 RS 集成重写（重要）：核实原 addon 绑定 `com.refinedstorage2.api.*` 旧包名在 RS 2.0.9（MC 1.21.1）已不存在（下载 jar 离线分析确认，集成实际失效）；`RtsRefinedStorageAddon` 重写为 `com.refinedmods.refinedstorage.api` 组件架构（BlockEntity.getContainerProvider→getContainers→getNode→getNetwork→getComponent(StorageNetworkComponent)→getResources/insert/extract，Actor.EMPTY 无权限门控），15 个绑定类/方法经 javap 逐一核对；插入/提取恢复可用。
- [21:30] 阶段一·1.3 残留清理：删 `rtsbuilding-main/build.gradle` JMH/SQLite 依赖与指向已删目录的 test exclude、删空目录 `server/workflow/event/`、删 `RtsbuildingMod`/`RtsBindingServiceImpl` 的 `ServiceRegistry` 误导 import、清 AE2 反射死代码（clGridHost 重复绑定、mhKeyGetDisplayStack 首次绑定被覆盖）。双份 RowLayout 经评估为不同用途同名类，保留。
- [22:00] 阶段一·1.1b 复查补漏（重要）：全仓 grep 核对发现阶段一遗漏 4 个同类协议脆弱点——`BuilderMode`（网络 SET_MODE）、`RtsStorageSort`（网络+存档 NBT）、`RtsWorkflowType`（工作流进度）、`RtsWorkflowPriority`（编码用 rank/解码用 ordinal 不对称）。三者改为显式 id + fromId，priority 补 fromRank 对称编解码；更新 `SessionSerializer`/`RtsPageCore`/`RtsPagePayloadFactory`/`RtsClientPacketGateway`/`ServerActionHandler`/`StorageState`/`WorkflowModule`/`RtsWorkflowSyncService` 调用点；新增 `ProtocolEnumTest`（12 测试全过）；补清 2 处 `ServiceRegistry` javadoc 残留（`RtsBlueprintServiceImpl`/`BlueprintTickPipe`）。全仓 `build` 通过。
- [23:00] 阶段二·Addon 集成统一抽象：api 新增 `RtsIntegration` SPI（integrationId/available/selfCheck/register）+ `RtsCompatRegistry.registerIntegration/getIntegrations`；AE2/RS/SB 反射 addon 实现 `selfCheck`（关键句柄 null 检查），4 个 addon 主类改 `implements RtsIntegration` 走统一注册入口；`RtsServer.init()` 增加 `checkIntegrations()` 统一健康检查（失败打 WARN）；gear 设置面板新增 `IntegrationSection` 可视化集成状态 + lang key；BD 新增 `BdAdapter` 收敛宿主 API 访问（清理 4 个宿主 import）；新增 `RtsCompatRegistryTest`（3 测试过）。gametest 因 dev 无宿主评估为不可行，以机制测试+运行时 selfCheck 覆盖。全仓 `build` 通过。
- [23:30] 阶段二·复查修正（3 处）：① `IntegrationSection` 状态列固定 70px 改动态宽度（防 "Refined Storage" 等长名重叠）、删未用常量/import、无宿主提示迁移 lang key；② AE2/RS/SB 反射加载失败时**不再跳过注册**——改注册带 `loadError` 诊断的 integration，使"宿主存在但绑定失败"在设置面板可见；③ lang 双文件新增 `ui.rtsbuilding.integration.none` 并核验 key 集合一致（zh 0/en 0 差异）。全仓 `build` 通过。
- [00:30] 阶段三·common 边界净化：① `RtsWorkflowStatus.typeLabel()`→`typeLabelKey()` 返回 lang key（common 零依赖 `Component`），UI 拼接移入 client `WorkflowRenderer.buildLabel`；② `RtsPinyinSearch` 加 `setDictionarySource` 注入 + `needsReload` 懒重载，main 在 `onServerStarting` 用 ResourceManager 注入真实资源源，新增 `RtsPinyinSearchTest`（3 测试过）；③ 核实 UI 状态持久化子系统（UiStateRepository/UiSnapshot/UiProperty/UiStateService/JsonFileRepository）零调用点，按删除分支删 5 文件 + 空 persist 目录。全仓 `build` 通过。
- [01:00] 阶段三·复查修正：`RtsPinyinSearch.ensureDictionaryLoaded` 原实现新来源加载为空时清空字典 → 改防御性保留原字典（避免 ResourceManager 首开失败导致拼音搜索静默失效）；`emptySourceYieldsNoMatches` 测试语义改为 `emptySourceKeepsExistingDictionary`（验证保留行为）；3 测试全过，全仓 `build` 通过。
- [02:00] 阶段五·测试与 CI 基建：新增 `.github/workflows/verify.yml`（JDK21 + 全仓 build + docs SPA 构建 + lang 校验，main/v2.0 分支 + PR）；新增 `scripts/verify-lang.ps1`（zh/en key 集合比对，本机通过+失败两路径验证）；新增 `RtsWorkflowStatusTest`（8 测试过：fromRaw 派生计算/holdType/typeLabelKey/null 防御）。网络 roundtrip 依赖完整 MC 运行时，由协议枚举测试+实机覆盖。全仓 `build` 通过。
- [03:00] 阶段四·服务端分层治理：① `RtsService` 加 init/shutdown/dependencies 生命周期，新增 `ServiceTopoSorter`（Kahn 拓扑排序，DFS 初版有 resolving bug 重写），`RtsServer` 拓扑 init + shutdown 接入 onServerStopped，`ServiceTopoSorterTest` 5 测试过；② 新增 `WorkflowState`/`WorkflowStateMachine`（common 纯逻辑状态机），`RtsWorkflowEntry.transition()` 校验合法转换并兼容 NBT 存档，token/engine 状态变更全改走 transition，`WorkflowStateMachineTest` 6 测试过；③ `BuildShape` 抽 21 个几何方法到 `ShapeGeometry`（797→378 行），BuildShapeFillModeTest 12 测试全过。全仓 `build` 通过，112 测试 0 失败。
- [04:00] 阶段六·打包与文档自动化：gradle.properties 新增 `builtin_mods`（5 个内置 addon 单一来源），`rtsbuilding-main/build.gradle` 的 `mods{}` sourceSet 与 `jar{}` 合并改为据此生成（定义前置）；新增 `addonManifest` 映射 + `verifyAddonPackaging` task（解包主 JAR 校验入口类合入 + toml modId 声明 + 未登记检测，挂 check），通过/失败两路径验证。全仓 `build` 通过。
- [05:00] 全项目扫描收尾：① ordinal 协议残留 0（grep 全仓确认）；② common 引用 main 专属类 0；③ 记忆清单 20 项全 `[x]`；④ 删 6 个空目录（blueprint/plugin/progression module、common/block、common/block.entity、server/service/crafting）；⑤ `IntegrationSection` 硬编码「宿主未加载」迁移 lang key（`ui.rtsbuilding.integration.unavailable`，zh/en 同步，lang 校验 189=189 通过）。`RenderingSection`/`BindingRenderer` 的 lang 迁移（AGENTS.md 待迁移项）记为独立待办，不并入六阶段。全仓 build 通过、112 测试 0 失败。
- [06:00] Lang 迁移专项：① `RenderingSection` 23 个 key（颜色标签+tooltip）迁移，新增静态 `tStatic()` 避免字段初始化顺序 NPE；② `BindingRenderer`/`RowLayout` 5 个 key（解绑/双向/仅提取/开启位置/关闭显示），ButtonBar 宽度同步；③ `BuildShape.hint` 10 个 key（含 %d 参数）+ `LineBrushSelector.currentHint` 由 replace("建造","破坏") 改为 `{action}`/`{button}` token 替换（建造/破坏、右键/左键由 lang 决定）。全仓 UI 硬编码复扫清零，lang 229=229 一致，全仓 build 通过、112 测试 0 失败。
- [08:00] UI 前置模组提取（大工程）：新增 `rtsbuilding-ui` 独立 mod（modId `rtsbuilding_ui`，基包 `com.rtsbuilding.rtsui`）——57 Java 类（render/animate/theme/state/window/component/color）+ 27 shader + 11 纹理（命名空间 rtsbuilding_ui）+ RtsUiMod 入口（9 shader 自注册）。解耦 3 点：BuilderScreen→RtsPanelHost 接口、FeatureAdjusterState 网络调用→setFunnelRadiusSync 回调注入、TopBarLayoutHelper.ButtonGroup→实现 ButtonGroupLayout 接口。主模块删 53 旧 UI 文件、58 文件 import 全量更新、toml 声明 rtsbuilding_ui + required 依赖。全仓 build 通过、112 测试 0 失败、jar 产物验证（UI 类/入口/shaders/纹理/toml）。
- [08:30] 修复运行崩溃：`RtsUiMod` 把 `RegisterShadersEvent`（IModBusEvent）误注册到 `NeoForge.EVENT_BUS`（common 总线）导致 mod 构造失败（"IModBusEvent events are not allowed on the common NeoForge bus"）→ 改注册到 `modEventBus`（MOD 总线）；删除未用 import。全仓 build 通过。
