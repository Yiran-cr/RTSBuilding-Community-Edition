# 2026-08-11 修改总结

- [HH:MM] XYZ 轴视角调节器：新增类似 Blender 的 XYZ 轴 gizmo（新增 AxisViewGizmo.java），左键/右键点击切到轴正负方向视角；CameraModule 新增 snapViewToAxis()（兼容自由/环绕/玩家环绕三种相机模式）；DownSidebarPanel 集成渲染与鼠标命中，BuilderScreen 增加 UI 命中判定，lang 增加中英文 tooltip。
- [HH:MM] XYZ 轴 gizmo 位置调整：从下板内部右上角改为悬浮于世界画面右下角（右栏左侧、下板之上），DownSidebarPanel 提取 layoutAxisGizmo() 统一渲染与命中判定位置。
- [HH:MM] XYZ 轴 gizmo 改版：从三固定按钮改为 Blender 风格轨道球（圆形面板 + 等距三彩轴 + 中心小球），点击轴切视角、按住中心球拖拽旋转（复用 queueRotateDrag），DownSidebarPanel 转发拖拽/释放事件，lang 增加拖拽提示。
- [HH:MM] XYZ 轴 gizmo 拖拽修复：修复中心点击被轴命中误判的问题——hitAxis 排除中心区域，mouseClicked 改为"轴命中优先、其余圆形面板内均可开始拖拽"，DownSidebarPanel.mouseClicked 先 layoutAxisGizmo 再判定。
- [HH:MM] XYZ 轴 gizmo 升级为 3D 轨道球：世界三轴按相机 yaw/pitch 正交投影到屏幕（right/up/forward 基底），滚盘随视角动态转动，背向观察者的轴变暗；点击朝向观察者的轴切视角、按住球面拖拽旋转。
- [HH:MM] XYZ 轴 gizmo 外观精简：移除外围半透明圆底与圆角边框，只保留轨道球本体（投影三彩轴 + 中心小球）。
- [HH:MM] XYZ 轴 gizmo 增加负轴：每个轴从中心向正负两端延伸（±X/±Y/±Z），负向轴端偏暗、独立悬停/tooltip；点击负端切换到负方向视角，lang 增加 nx/ny/nz 键。
- [HH:MM] XYZ 轴 gizmo 线段改为矢量渲染：用 pose 旋转 + SDF 圆角胶囊线（drawLineVector）替代逐像素 Bresenham fill。
- [HH:MM] XYZ 轴 gizmo 添加圆形背景：渲染轴前先绘制半透明圆形底与外圈圆角边框，拖拽时边框高亮。
- [HH:MM] XYZ 轴 gizmo 移除中心大圆点：删除坐标轴交汇处的白色高亮圆绘制。
- [HH:MM] XYZ 轴 gizmo 背景简化：仅保留圆形半透明底，移除外圈圆角边框。
- [HH:MM] XYZ 轴 gizmo 深度测试：六段轴按投影深度排序（画家算法）绘制，背向观察者的轴段仍绘制但变暗，被朝向轴段覆盖（遮挡正确），恢复 BACK_DARKEN。
- [HH:MM] XYZ 轴 gizmo 轴端字母加圆形背景：背景圆用轴色（与文本颜色一致），字母白色居中。
- [HH:MM] XYZ 轴 gizmo 元素越界修复：轴段长改 AXIS_LEN=17、字母圆点半径 6 且圆心在轴端，所有元素（线段+圆点最远 23）容纳在背景圆 24 内，不再渲染到背景外。
- [HH:MM] XYZ 轴 gizmo 背景圆放大并自适应：RADIUS=32，面板尺寸/轴段长按背景圆自动推导（WIDTH=(RADIUS+4)*2、AXIS_LEN=RADIUS-LABEL_DOT_RADIUS-2）。
- [HH:MM] XYZ 轴 gizmo 背向轴段处理：背向观察者的轴段不绘制线段（被球体遮挡），但轴端字母圆点背景仍显示（变暗色）。
- [HH:MM] XYZ 轴 gizmo 文字与悬停调整：正轴文字/圆点始终显示（背向变暗），负轴不显示文字圆点；线段不随悬停高亮，仅轴端球悬停时变白色、文字转深色。
- [HH:MM] XYZ 轴 gizmo 正负轴视觉区分：负轴不绘制线段，仅绘制无文字的背景圆；正轴绘制线段与带文字圆点；悬停球白色高亮保留。
- [HH:MM] XYZ 轴 gizmo 命中改圆点判定：悬浮/点击按轴端文字背景圆圆心距离判定（半径+2），点击线段无反应；拖拽旋转仅从中心小球区域开始；清理 AXIS_HIT_TOLERANCE 与 distanceToSegment 死代码。
- [HH:MM] XYZ 轴 gizmo 正轴线段始终渲染：正轴 XYZ 线段不再因背向观察者隐藏（背向时用变暗色），负轴仍不绘制线段。
- [HH:MM] XYZ 轴 gizmo 拖拽恢复：整个圆形背景范围内均可按住拖拽旋转视角（点击线段仍不触发轴跳转）。
- [HH:MM] XYZ 轴 gizmo 背向圆点可点击：hitAxis 移除朝向限制，正负六段文字背景圆无论背向/朝向均可悬浮与点击跳转。
- [HH:MM] XYZ 轴 gizmo 三档高亮：鼠标悬浮大圆背景时全部轴段/圆球整体提亮；相机朝向对应轴段更高亮（computeActiveSegment 取 forward 点积最大轴段）；鼠标悬浮的球变白色高亮。
- [HH:MM] XYZ 轴 gizmo 朝向高亮改轴掩码：computeActiveAxisMask 按各轴 |axis·forward|≥0.5 判定，对齐 X 只亮 X、处于 XY 平面亮 X/Y、三维斜向三轴全亮。
- [HH:MM] XYZ 轴 gizmo 朝向高亮区分正负：computeActiveSegmentMask 用 6 位段掩码，环绕模式观察向量=相机位置−目标（在哪一侧亮哪一侧），自由视角用视线方向，轴段方向点积≥阈值才高亮。
- [HH:MM] XYZ 轴 gizmo 透视缩放：轴段长度/圆点半径/文字按深度近大远小（perspectiveScale 0.55~1.25），AXIS_LEN 按最大缩放推导保证元素在背景圆内，hitAxis 与 tooltip 锚点同步透视。
- [HH:MM] XYZ 轴 gizmo 移除背向变暗逻辑：删除 BACK_DARKEN/NEGATIVE_DARKEN 常量与相关颜色缩放，正负轴段仅通过透视缩放与高亮体现层次。
- [HH:MM] XYZ 轴 gizmo 透视平滑修复：轴段端点固定在球面（长度恒定），球与文字改用 pose 浮点缩放，消除端点伸缩与 int 取整造成的跳变。
- [HH:MM] XYZ 轴 gizmo 高亮亮度调低：悬浮 ×1.10、朝向轴 ×1.25（叠乘 ≈1.38），避免原先叠乘 ×2.3 过曝。
- [HH:MM] XYZ 轴 gizmo 透视范围收窄：0.55~1.25 改为 0.80~1.15，背向轴段不过度缩小，透视更自然。
- [HH:MM] XYZ 轴 gizmo 环绕跳转动画：snapOrbitToAxis 改为启动 450ms 角度插值动画（水平角最短弧、俯仰线性、缓出曲线），onRenderFrame 逐帧推进并由 processOrbitInput 重算位置；用户手动操作相机时取消动画。
- [HH:MM] XYZ 轴 gizmo 自由视角跳转动画：snapFreeViewToAxis 改为 450ms yaw/pitch 插值（偏航最短弧、缓出），onRenderFrame 自由分支逐帧推进，手动操作时一并取消。
- [HH:MM] XYZ 轴 gizmo 遮挡与翻转逻辑：hitAxis 重叠命中选深度最大（最前方）按钮实现遮挡；mouseClicked 当相机正对按钮方向（观察向量点积≥0.9）时点击翻转跳转到反方向，否则跳转到按钮方向；提取 computeViewVector 复用。
- [HH:MM] XYZ 轴 gizmo 拖拽隐藏光标：拖拽开始时 GLFW 隐藏光标并记录物理坐标，释放时恢复光标并移动回进入位置；DownSidebarPanel 增加 releaseAxisGizmoCursor，BuilderScreen.onClose 退出时确保恢复。
- [HH:MM] XYZ 轴 gizmo 拖拽抑制世界交互：拖拽期间禁用 gizmo 文字球悬浮判断；InteractionTargetPass（点击模式）与 BoxSelectionPass（框选 hover 预览）在 gizmo 拖拽时跳过渲染；BuilderScreen 新增 isAxisGizmoDragging 查询。
- [HH:MM] XYZ 轴 gizmo 拖拽限左键：mouseClicked 拖拽分支增加 button==0 判断，右键点击圆内不再进入拖拽、不隐藏光标。
- [HH:MM] XYZ 轴 gizmo 重构：615 行上帝类拆分为 4 个类——TrackballProjection（3D 投影数学 + 统一 projectScreen）、AxisGizmoState（可变状态）、AxisGizmoRenderer（渲染+tooltip）、AxisGizmoInputHandler（命中+输入+光标）；AxisViewGizmo 改为门面组合；动画状态机从 CameraModule 抽出为 CameraViewSnapController，CameraModule 仅保留 snapViewToAxis 委托。
- [HH:MM] XYZ 轴 gizmo 像素阶梯修复：SegmentScreen 坐标改 double、drawLineVector 改浮点入参、pose.translate 全程浮点，避免低分辨率/GUI 缩放下球心与端点按像素跳跃；tooltip 锚点单独取整。
- [HH:MM] XYZ 轴 gizmo 正负轴方向修正：updateViewBasis 的 right 从 (cosYaw,0,sinYaw) 改为 (-cosYaw,0,-sinYaw)，纠正 MC 右手系屏幕方向，正负轴不再镜像互换。
- [14:46] XYZ 轴 gizmo 链路检查修复：轴段跳转限定左键、DownSidebarPanel gizmo 区域内点击统一消费防右键框选；LineBrushRenderPass 拖拽期间跳过；CameraViewSnapController.advance 增加模式切换取消动画保护；hitAxis 移除重复 updateViewBasis。产出 docs/reports/xyz-axis-gizmo.json，编译+测试通过。
- [14:54] XYZ 轴 gizmo 移除悬浮提示逻辑：删除 AxisGizmoState 的 tooltips/centerTooltip 字段与 getter、AxisGizmoRenderer 的 renderTooltipOverlay/axisTooltipKey/hoverCenter 及 TooltipController/Component/DarkUiPalette import、AxisViewGizmo 的 renderTooltipOverlay 门面、DownSidebarPanel 的调用；清理 zh_cn/en_us lang 中 7 个 axis_gizmo tooltip key，TrackballProjection 注释同步更新。编译+测试通过。
- [15:08] 无人机显示玩家名字：RtsCameraEntityHelper.createAndSpawnDrone 创建无人机后使用原生态实方法 setCustomName/setCustomNameVisible 显示所属玩家名字（通过 level.getPlayerByUUID 查玩家）。编译通过。
- [15:20] 无人机碰撞箱对齐模型：RtsDroneEntity.getDimensions 由零尺寸改为匹配模型尺寸（1.4×0.5），snapTo/moveTowardTarget 位置变化后 setBoundingBox(makeBoundingBox())，使模型中心与碰撞箱中心重合、名字标签显示在模型上方。编译通过。
- [15:40] 无人机碰撞箱中心对齐修复：查明"模型偏下"根因是原版碰撞箱 Y 范围 [getY(), getY()+height] 以底部为基准、而模型以实体位置（中心）为基准；RtsDroneEntity 重写 makeBoundingBox 为中心对称，getDimensions 扩为 2.2×0.6 完整包住模型（含螺旋桨叶片）。编译+测试通过。
- [16:29] 无人机建造/破坏光束：新建 S2CRtsDroneBeamPayload 网络包 + RtsDroneBeamService（服务端广播，只发其他玩家、每 tick 每玩家限流 4 条）+ DroneBeamRenderer（客户端世界渲染，按无人机实体 ID 实时追踪摄像头/方块端点，蓝=建造/红=破坏，4 秒后消失）；挂载到 RtsPlacementSound.playRemotePlacedBlockAnimation、RtsMiningNetworkHelper.sendBreakAnimation、RtsBlueprintServiceImpl.noteBlockPlaced。RtsCameraManager 新增 getDroneEntityId/getDroneCameraPosition，RtsDroneEntity 新增 CAMERA_PART_OFFSET 常量。编译+测试通过。
- [16:47] 无人机光束改版为"发射→命中→收回"一次性动画：DroneBeamRenderer 重写——280ms 光球沿射线缓出发射（白芯+彩色侧晕双层激光、前端光球十字），220ms 命中全亮+目标端命中闪光，200ms 整条渐隐收回（命中后即收回，不再持续 4 秒）；建造蓝从摄像头射向方块、破坏红从方块射向摄像头。编译+测试通过。
- [15:40] 无人机动画联机同步修复：RtsDroneEntity.sendAnimStateToOwner 改为 sendAnimStateToTracking，经新增 Platform.sendToPlayersTrackingEntity（PacketDistributor.sendToPlayersTrackingEntity）广播 S2CRtsDroneAnimPayload 给所有追踪玩家；并加 ANIM_RESEND_INTERVAL=20 tick 周期强制重发兜底新进入范围玩家。产出 docs/reports/drone-animation.json，编译+测试通过。
- [17:05] 边界屏障渲染链路检查修复（跟随摄像机阴影）：ShaderState 光影检测改保守模式（loader 存在但无法证明关闭即视为激活，新增 iris.propertiesDefinitivelyOff/OptiFine 明确关闭判定）；RenderPipeline 对 BoundaryPass 增加光影态硬跳过兜底；RtsClientKernel 新增 resetRegion()，RTS 关闭时复位 regionValid 防墙残留；BoundaryPass 墙 alpha 0.8→0.35、删除 isConfigSafe 死代码。产出 docs/reports/boundary-render-chain.json，编译+测试通过。
- [17:13] 边界屏障阴影二次修复（墙高超长+锚点污染）：BoundaryPass 墙高封顶 [anchor.y-4, min(边界最高+5, anchor.y+20)] 消除从 -64 到百格高空的超长墙体；ShaderState.isIrisPresent 覆盖多 Iris 包名；TemporaryContextSwitcher 增加 temporarilySwitchingPosition 标记、RtsCameraManager.updateAnchorForPlayer 在临时位置切换窗口内跳过，防锚点被虚拟交互位置污染导致墙体跳动。更新 docs/reports/boundary-render-chain.json（问题 7 修复 6 保留 1），编译+测试通过。
- [17:24] 边界屏障阴影三修（运行日志实锤）：根因1 ShaderState 检测解析 iris.properties 只认旧键 shaderPackName=，而 Iris 1.8.14 实际写 shaderPack=，启用判定恒 false，光影防护失效；改为兼容双键并新增 ModList.get().isLoaded(iris) 原生检测（规避 NeoForge 模块化 classloader 下 Class.forName 跨模块失效）。根因2 玩家出生 y=-60 虚空下落，updateAnchorForPlayer 用三维距离跟随 y 导致锚点/墙/相机联动下移（墙跟随摄像机实锤），改为仅水平跨格更新、锚点 y 保持开启值。更新 docs/reports/boundary-render-chain.json（问题 8 修复 7 保留 1），编译+测试+build 通过。
- [18:15] 光影下边界屏障改为线框渲染：BoundaryPass.render 按 ShaderState.isShaderPackActive() 分派——光影激活用 alloc.lines()（POSITION_COLOR 线条）绘制 4 根角柱+顶/底各 4 条边（不受光影光照/阴影采样影响，无暗带伪影），原版管线仍渲染半透明墙；shouldRender 不再因光影整体禁用，RenderPipeline 移除 BoundaryPass 光影硬跳过兜底，墙高计算提取 resolveWallY 两路径复用。更新 docs/reports/boundary-render-chain.json（问题 9 修复 8 保留 1），编译+测试+build 通过。
- [18:52] 光影下边界改回半透明墙（POSITION_COLOR 直通）：BoundaryPass 光影分支用 alloc.brackets()（自定义 POSITION_COLOR+半透明+LEQUAL 深度，不采样光照/阴影，Iris 对 basic 管线通常直通）画四堵纯色半透明墙（无纹理滚动），替换上一版线框方案；原版管线仍用 entityTranslucent 贴图墙。编译+测试+build 通过，报告与 change-log 更新。
- [18:55] 模组改名：mod_name 从 'RTS building Build From Above' 改为 'RTS Building: Community Edition'（gradle.properties，经 mod_name 注入 neoforge.mods.toml 主 mod displayName，modId rtsbuilding 不变）；README.md 中英文标题/描述与 AGENTS.md 标题同步。重建 jar 验证 displayName 生效。
- [19:01] 边界墙改回带贴图渲染（统一无光照）：RenderPipeline 屏障 RenderType 从 entityTranslucent 改为自定义 POSITION_TEX_COLOR + GameRenderer.getPositionTexColorShader()（position_tex_color，unlit 不采样光照/阴影，Iris 对 basic 管线通常直通），带 barrier.png 贴图+半透明+LEQUAL；BoundaryPass 移除光影分派/纯色/线框分支，addTexturedQuad 改 setColor+setUv（POSITION_TEX_COLOR 无 lightmap/normal），原版与光影下统一显示带滚动贴图的半透明墙。编译+测试+build 通过。
- [20:21] 无人机名字标签位置修复：RtsDroneEntity 碰撞箱改为中心对称（getY=模型中心）后，原版 EntityAttachment.NAME_TAG 默认 (0, getBbHeight(), 0)（底部基准）使名字标签偏高一个碰撞箱半径（getBbHeight 0.6 → 标签 y=1.1）。getDimensions 改用 EntityDimensions.withAttachments 把 NAME_TAG 附件锚定到模型中心 (0,0,0)，renderNameTag 再 +0.5 → 标签位于模型上方合适位置。编译+测试+build 通过。
- [20:41] 容器网页标签面板链路检查修复（实体/方块容器冲突 + 标签无名）：EntityInteractionHandler 单点实体容器（村民等）改为与方块一致先 recordDirectInteract 记录标签再发交互包，消除实体菜单被 openContainerPage 错误关联到旧方块 activeId 的冲突；InteractionPanel.buildTabs 外部容器标签改取屏幕真实标题（不再固定「容器面板」）、缓存条件加 tabsCacheScreen 屏幕实例；ContainerPageState 新增 pendingPromoted 标记区分 tick 转正与外部打开。产出 docs/reports/container-page.json，编译+测试通过。
- [21:05] 撤回发射无人机激光：HistoryExecutor 撤回破坏（restoreBlocks 恢复放置）广播建造蓝光、撤回放置（breakBlocks 破坏方块）广播破坏红光，经 RtsDroneBeamService.broadcastPlace/broadcastBreak（复用每 tick 每玩家 4 条限流）。编译+测试通过。
- [21:35] 右面板下嵌层功能调节器（漏斗吸取范围 + 连锁挖掘数量）：新建 FeatureAdjusterState（客户端配置，漏斗半径同步服务端）；LowerRightOverlayLayer 渲染两行滑块（漏斗/连锁启用时显示），ScaleSliderComponent 增加轨道点击跳转；RightSidebarPanel 转发 scroll/drag/release 到 layers；RtsFunnelService 支持每玩家半径（ActionType 追加 SET_FUNNEL_RADIUS + gateway + ServerActionHandler）；BuildInteractionHandler/UltiminePreviewPass 改用调节后的连锁数量、FunnelRangePass 改用调节后的漏斗半径。编译+测试+build 通过。
- [21:50] 调节器粒度细化：连锁挖掘数量步进 16→1（1 个 1 个增减），漏斗吸取范围粒度 0.5→0.1；ScaleSliderComponent.handleScroll 增加可配置 step 重载，下嵌层漏斗行滚轮 0.1、连锁行滚轮 1.0。编译+测试通过。
- [22:05] 滚轮粗调：ScaleSliderComponent.handleScroll 按住 Shift 时步进 ×10（连锁 1→10、漏斗 0.1→1），所有使用该组件的滑块（下嵌层调节器/Gear 菜单）统一生效。编译+测试通过。
- [22:20] 下嵌层调节器独立背景框：LowerRightOverlayLayer 每个调节器用 drawBorderedRoundedRect 圆角半透明框包裹，标题与滑块都在框内、框间留隙，区分漏斗/连锁两个调节器；轨道位置随框内布局重算供命中复用。编译+测试通过。
- [22:40] 调节器数值支持文本输入：新建 NumericInputBox（数字/小数点输入、回车提交/Esc 取消/退格/方向键/Ctrl+V、光标闪烁）；LowerRightOverlayLayer 标签右侧值改为输入框（漏斗 double、连锁 int，提交自动收敛到合法范围），点击输入框外自动提交；RightSidebarPanel 转发 keyPressed/charTyped 到 layers。编译+测试通过。
- [22:55] 修复调节器文本无法输入：根因是 renderContent 每帧调用 createFunnelInput/createUltimineInput 新建 NumericInputBox 实例，编辑状态每帧被丢弃，键盘事件总是落在 editing=false 的实例上。改为字段持例（funnelInput/ultimineInput 只创建一次）。编译+测试通过。
- [23:05] 调节器背景框配色：LowerRightOverlayLayer 背景框填充色从半透明黑（0x33000000）改为 DarkUiPalette.p6()（dark.png 第 7 格色），边框仍用 border。编译通过。
- [23:35] 体/球/圆柱填充模式（实心/空心/框架）：新建 FillMode 枚举；ShapeParams 持 fillMode（reset 不重置）、ShapeInput 增加字段、LineBrushSelector 提供 get/cycleFillMode 并纳入计算缓存；BuildShape 的 SOLID/CIRCLE/SPHERE compute 按模式分发——体框架=12 棱、圆柱框架=侧壁薄壳、球框架=赤道环+两条子午环（hollow=外壳薄层，通用 6 邻域过滤）；ShapeButtonGroup 底部加「模式」切换按钮、RtsKeyMappings 加 CYCLE_FILL_MODE_KEY（X 键）、BuilderScreenEventRouter 接快捷键、lang 双语、hint 显示当前模式。新增 BuildShapeFillModeTest 5 用例。编译+测试通过。
- [23:50] 填充模式调节移到右面板下嵌层：ShapeButtonGroup 恢复原状（移除左栏按钮）；LowerRightOverlayLayer 新增「填充模式」背景框（实心/空心/框架三按钮，当前模式高亮，点击直接选择），显示条件=当前形状为体/球/圆柱（BuilderScreen 新增 isFillModeShapeActive）；LineBrushSelector 新增 setFillMode；X 键快捷键保留。编译+测试通过。
- [00:05] 填充模式按形状独立记忆：LineBrushSelector 改用 EnumMap&lt;BuildShape,FillMode&gt; 按形状存储（get/set/cycleFillModeFor），ShapeParams 移除共享 fillMode、BuildShape hint 移除 fillModeHint；BuilderScreen 新增 getActiveBuildShape()，下嵌层按钮与 X 键快捷键均基于当前激活形状读写各自模式。编译+测试通过。
- [00:25] 形状模式扩展：FillMode 增加 CONNECTED（线·连接）/SEPARATED（线·断点），modesFor(shape) 定义各形状子集（体/圆柱/球=实心/空心/框架；墙/面=实心/框架；线=连接/断点）；BuildShape 的 LINE 连接模式走 connectedLineBetween（逐轴直角连接、禁止斜向），WALL/FACE 增加框架模式（矩形四边框 wallFramePositions/faceFramePositions）；下嵌层标题改「形状模式」、按钮按形状子集动态渲染（BuilderScreen.isFillModeShapeActive 更名 isShapeModeActive 并覆盖全部形状）。新增 5 个测试。编译+测试通过。
- [00:40] 线连接模式改为管道式：connectedLineBetween 从"先走完 X 再 Y 再 Z"的集中拐角改为"每步选择距目标最远的轴推进"的贪心管道路径——相邻格恒共享面、路径贴近两点直线、形似接管道蜿蜒；新增 lineConnectedIsPipeLikeAndFaceAdjacent 测试（验证相邻格曼哈顿距离恒 1）。编译+测试通过。
- [00:48] 线默认模式改为断点：FillMode 新增 defaultFor(shape)（线默认 SEPARATED、其余 SOLID），LineBrushSelector.getFillModeFor 未设置时返回形状默认；新增 lineDefaultsToSeparated 测试。编译+测试通过。
- [00:55] 形状模式切换改现代分段控件：LowerRightOverlayLayer 的 renderFillRowBox 从旧式 drawButtonBg 按钮改为 iOS 风格分段控件——圆角胶囊容器（SEG_BG_COLOR）+ 选中段主题高亮胶囊（DarkUiPalette.toggleOn，SEG_INSET 内缩）+ 180ms EASE_OUT_QUAD 滑动动画（AnimFloat fillModeSlide）+ 未选中段悬停文字渐亮。编译+测试通过。
- [01:10] 方块替换开关 + 黄光：LineBrushSelector 新增 replaceBlocks 全局开关（toggleReplaceBlocks/isReplaceEnabled，所有形状共享）；下嵌层「形状模式」框标题行右侧（分段控件上方）新增「替换：开/关」胶囊按钮；BuildInteractionHandler 的 forcePlace 改为 isReplaceModeActive()（Shift 或开关），框选批量放置 collectAreaPositions 支持替换（replace 时收集全部位置）；DroneBeamRenderer 替换开启时建造光束改黄色（REPLACE_COLOR）。编译+测试通过。
- [01:25] 替换逻辑改为逐个破坏再逐个放置：RtsPlacementQuickBuild.placeStateBatchEntry 增加 replace 参数，目标被非空气方块占用且替换开启时先 breakForReplace（破坏红光动画 + 移除方块 + 物品退还链接存储/背包/掉落物）再放置新方块，批量循环内形成"破坏一个、放置一个"交替顺序；RtsPlacementBatch 传 job.forcePlace() 并调整实际放置判定（forcePlace 覆盖时计入 placedPositions）。编译+测试通过。
- [01:35] 形状模式标题显示当前形状：BuildShape 新增 label()（线/墙/面/体/圆柱/球），下嵌层「形状模式」框标题改为「形状模式：线」等动态显示。编译+测试通过。
- [01:42] 移除下嵌层空态占位文本：删除 LowerRightOverlayLayer 的「启用漏斗 / 连锁挖掘后显示调节器」提示，与其他嵌层（ContainerBinding/ItemGrid/WorkflowProgress）一致——无调节器时保持空白。编译+测试通过。
- [01:55] 下嵌层插件化统一嵌层风格：调节器逻辑从 LowerRightOverlayLayer 提取到 plugin 包新建的 FeatureAdjusters（持有 OverlayContext，与 ContainerBinding/ItemGrid/WorkflowProgress 同构），LowerRightOverlayLayer 改为薄转发层。编译+测试通过。
- [02:05] 形状模式调节器仅建造模式显示：BuilderScreen.getActiveBuildShape() 改为只在建造模式（isConstructionSelected）返回形状，破坏模式返回 null——下嵌层形状模式框与 X 键仅在建造模式生效。编译+测试通过。
- [02:15] 下嵌层空态占位提示：FeatureAdjusters 无调节器时居中显示半透明占位文本，颜色 `(textColor & 0xFFFFFF) | 0x60000000`，与 GridRenderer 搜索框 placeholder 风格一致；文案后改为「暂无调节器」。编译+测试通过。
- [02:25] 替换开关改用设置面板 ToggleSwitch 风格：FeatureAdjusters 的替换按钮从自定义 drawPill 胶囊改为复用 ToggleSwitch 组件（轨道+滑块动画、外部状态驱动），开关前加半透明「替换」小标签。编译+测试通过。
- [02:40] 形状模式框重布局：FeatureAdjusters 改为三行独立布局（标题行「形状模式：xxx」/ 替换开关行「替换 + ToggleSwitch」/ 分段控件行全宽胶囊），fillRowH 单独计算（标题+开关+分段各占一行留足间距），背景框内元素不再拥挤。编译+测试通过。
- [02:52] 单方块模式支持方块替换按钮：BuilderScreen.isShapeModeActive 更名 isShapeAdjusterActive（建造模式激活即显示，含单方块）；FeatureAdjusters 标题单方块显示「形状模式：单方块」，替换开关始终绘制，分段控件仅非单方块形状显示（fillRowH 动态）。编译+测试通过。
- [03:05] 线连接模式改为"断点基础上补连接"：connectedLineBetween 不再贪心重排路径，改为以 3D DDA 断点路径为基底、逐对在斜向断开处按顺序补中间连接块（三轴对角补两块），路径走向不变、全程面连接；lineConnectedIsPipeLikeAndFaceAdjacent 测试更新。编译+测试通过。
- [03:10] 线模式按钮顺序互换：FillMode.modesFor(LINE) 从 [连接,断点] 改为 [断点,连接]，下嵌层分段控件左侧为「断点」、右侧为「连接」；modeSubsetsPerShape 测试同步。编译+测试通过。
- [03:20] 形状/单方块调节框严格限定建造模式：BuilderScreen.isShapeAdjusterActive 增加 isBuildMode()（顶栏 BUILD）前置条件，破坏/交互/蓝图模式下不再显示（含单方块替换开关）。编译+测试通过。
- [03:30] 破坏模式按放置模式实现调节器：BuilderScreen.isShapeAdjusterActive 扩展为建造或破坏侧激活均显示；getActiveBuildShape 恢复破坏侧返回（getBreakShape），破坏模式同样有形状模式分段控件与替换开关，X 键同样生效。编译+测试通过。
- [03:38] 修复形状调节器显示时仍出现「暂无调节器」：rows 只记录漏斗/连锁滑块行，形状调节器不计入，占位判断改为 `!showFillMode && rows.isEmpty()`，有形状调节器时不再显示占位。编译+测试通过。
- [03:50] 单方块替换实现 + 破坏模式隐藏替换开关：RtsPlacementQuickBuild.breakForReplace 改 public 供单方块交互放置路径复用，RtsPlacementExecutor 在 forcePlace 且不跳过占用时先破坏目标再走 placeWithStorageItem；FeatureAdjusters 替换开关行仅建造侧显示（fillRowH 动态），破坏模式不再出现替换按钮。编译+测试通过。
- [04:05] 替换破坏改用原生破坏逻辑 + 无物品不破坏：删除自定义 breakForReplace，快速建造 placeStateBatchEntry 改为「先提取物品（确认可用）→ 再 RtsMiningStateMachine.destroyMinedBlock 原生破坏 → 再放置」，提取失败不破坏、破坏后无法放置退款；单方块 RtsPlacementExecutor 增加 hasItemAvailable 预检（背包/链接存储/创造恒 true）后才破坏。编译+测试通过。
- [04:45] 工作流恢复面板：新增 network/resume 三个包（C2SRequestResumeScan/C2SResumeAction/S2CResumeScan + RtsResumePackets 注册）；ResumeWorkflowService 统一扫描（蓝图多材料/普通单物品 + 剩余/冲突位置）与恢复动作（0开始/1跳过/2覆盖，覆盖用原生 destroyMinedBlock）；客户端 ResumeWorkflowPanel 浮动窗口（材料清单+统计，无冲突够材料→开始、不足→灰、有冲突→跳过/覆盖）、ResumePreviewPass（剩余绿线框/冲突橙线框）、WorkflowInputHandler 暂停工作流点「继续」→ 请求扫描弹面板。编译+测试通过。
- [05:00] 恢复线框清理：ResumeWorkflowPanel.onClose（关闭按钮/Esc/动作后）清空 ResumeWorkflowState 移除线框并释放单例；handleWorkflowProgress 检测到当前恢复工作流完成（completed+failed≥total）时同样清空状态并关闭面板。编译+测试通过。
- [05:15] 恢复面板按工作流独立：ResumeWorkflowState 改 Map&lt;entryId,payload&gt;、ResumeWorkflowPanel 改 Map&lt;entryId,panel&gt;（每个工作流独立实例，同时打开互不干扰），onClose 只移除自身 entryId；ResumePreviewPass 渲染所有打开工作流的线框；handleWorkflowProgress 完成检测按 entryId 精准清理。编译+测试通过。
- [05:22] 跳过/覆盖按钮材料不足时禁用：ResumeWorkflowPanel 跳过/覆盖按钮与开始按钮一致，missingItems&gt;0 时灰色化且点击无效。编译+测试通过。
- [05:35] RTS UI 自动跟随原版 GUI 缩放：BuilderScreenScaleManager 默认 autoRtsGuiScale（getRtsGuiScale 返回 mc.getWindow().getGuiScale()），窗口变化时 UI 与原版一致缩放重排（renderScale≈1 时不再额外缩放）；用户手动 adjust/set 切固定模式，新增 resetRtsGuiScale/resetToAutoRtsGuiScale 恢复自动，label 显示「自动」。编译+测试通过。
- [05:48] 深度测试线框透明度全部改 100%：UltiminePreviewPass/ResumePreviewPass EDGE_ALPHA 0.92→1.0、LineBrushRenderPass depthAlpha 0.9·flicker→1.0、BoxSelectionPass DEPTH_ALPHA 0.9→1.0（实体/容器角支架 0.9/0.6→1.0）、InteractionTargetPass 目标角支架 0.9→1.0；noDepth 穿透层保持原样。编译+测试通过。
- [06:05] 单方块替换修复 + 掉落物进存储：RtsPlacementExecutor 单方块替换改为快速建造式可靠放置（placeReplaceAt 构建 StatePlacementPlan 走 placeStateBatchEntry：先提取物品确认→原生破坏→放置，消除"挖空但没放上"的失效，移除 hasItemAvailable 预检）；RtsPlacementQuickBuild 替换破坏后调用 RtsDropAbsorber.absorbNearbyMinedDrops 把掉落物直接存入 RTS 存储（与正常挖掘一致）。编译+测试通过。
- [06:20] 恢复面板布局美观优化：ResumeWorkflowPanel 加宽（288×196）、材料行渲染物品图标（g.renderItem）+ 名称 + 「×N（有 M）」右侧对齐，可用量足够绿/不足红、统计行缺料红/冲突橙，分隔线分隔材料区，按钮加高。编译+测试通过。
- [06:28] 创造模式材料可用量显示 ∞：ResumeWorkflowPanel 材料行在玩家创造模式时「有 ∞」替代 int 最大值，颜色恒绿。编译+测试通过。
- [06:35] 恢复面板物品图标遮挡修复：renderItem 改为带 z 重载 `g.renderItem(stack, x, y, 0)`，图标与面板内容同层可被其他 UI 正常遮挡/裁剪（默认 renderItem 的 z=100 会浮在其他 UI 之上）。编译+测试通过。
- [07:00] 硬编码 UI 文案迁移 lang + AGENTS 约定：扫描项目（492 Java 文件，233 行含中文，其中约 60 处用户可见 UI 文案）。迁移到 zh_cn/en_us lang：ResumeWorkflowPanel、FeatureAdjusters、FillMode/BuildShape label、InteractionPanel、BuilderScreenScaleManager 自动、蓝图前缀、工作流恢复/暂停/等待/满提示、工作流类型 label、搁置后缀、截断消息；AGENTS.md 新增「语言文件（lang）约定」章节（key 前缀、取词方式、不迁移范围、待迁移清单）。编译+测试通过。
- [07:10] 恢复面板物品图标按 GridRenderer 方式渲染：显式 RenderSystem.disableDepthTest → push/translate/renderItem → pop → enableDepthTest，图标随面板 scissor 裁剪、可被其他 UI 遮挡且不穿透。编译+测试通过。
- [07:25] 修复恢复面板图标穿透：根因是 renderItem 后自行 RenderSystem.enableDepthTest 残留开启，后续浮动窗口在深度测试下被图标 z 挡住。改为与 PageTabBar 一致——不显式管理 RenderSystem（renderItem 内部禁用深度、按绘制顺序覆盖），图标可被其他 UI 正常遮挡。编译+测试通过。
- [07:45] 图标穿透二次修复：确认现象为「后打开面板盖不住恢复面板图标」，根因是 renderItem 内部会重新启用深度测试并绘制在 z=100，残留开启导致后续窗口被图标深度挡住。改为 renderItem 绘制后显式 RenderSystem.disableDepthTest（与 GridRenderer 最终保持禁用一致）。编译+测试通过。
- [08:00] 图标穿透彻底修复：弃用 renderItem，改为获取物品 BakedModel 的粒子精灵（TextureAtlasSprite）用 GuiGraphics.blit 按普通 GUI 批次纹理绘制——完全受 scissor 与绘制顺序控制，不再存在独立渲染/深度残留，可被其他浮动窗口正常遮挡。编译+测试通过。
- [08:15] 恢复面板物品图标改为与 interaction 面板（PageTabBar）完全一致的绘制：pushPose + translate(图标中心) + scale(ICON/16) + renderItem(-8,-8) + popPose。编译+测试通过。
- [08:30] 恢复面板图标改回 renderItem 真实物品图标：此前改用 TextureAtlasSprite + blit 显示为粒子贴图（用户要求图标）；现用 renderItem 绘制真实物品模型图标，绘制前后显式 disableDepthTest 防止深度残留导致后续 UI 无法遮挡。编译+测试通过。
- [08:40] 完全移除恢复面板物品图标：ResumeWorkflowPanel 材料行仅显示名称 + 「×N（有 M）」，删除图标绘制、itemStackOf 方法、ICON_SIZE 常量与 ItemStack import。编译+测试通过。
