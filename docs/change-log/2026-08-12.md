# 2026-08-12

- [15:00] 物品图标绘制统一：新增 `client/util/render/GuiItemRenderer.java` 统一物品图标绘制逻辑（深度测试成对管理 + 居中缩放绘制），收敛 `GridSlotRenderer.drawIcon`、`GridRenderer` 当前选中、`BuilderScreen` carried、`BindingRenderer.renderItemIcon`、`PageTabBar.renderItemIcon` 五处 `renderItem` 调用点，消除原先 depth 处理不一致/裸调问题。
- [15:30] 工作流恢复面板绘制材料图标：`GuiItemRenderer` 新增 `resolveItemStack`（id→ItemStack 解析），`ResumeWorkflowPanel.renderMaterialRow` 每行材料左侧绘制物品图标并右移名称起点。
- [16:00] 修复物品图标透过上层面板：`GuiItemRenderer` 物品绘制完成后 flush+清空深度缓冲（新增 `drawItemBatch`/`finishItemBatch` 批量路径供网格高频场景），修复深度缓冲污染导致的穿透问题。
- [16:40] 工作流恢复面板材料行加进度条：`ResumeWorkflowPanel.renderMaterialRow` 改为"图标+名称+右侧数量"顶部行与进度条双行布局，用 `SdfRenderer.drawProgressBar` 将拥有/缺少比例具象化（充足绿渐变、不足红渐变），行高 19→26、面板高 196→218。
- [17:10] 恢复面板材料行布局重构：进度条内直接叠加文字（左名称 + 右「有 M / 需 R」，不足追加红色「缺 X」），行高 26→20、面板高 218→190，lang 新增 `material_avail_req`/`material_missing_item` 并删除旧 `material_row`。
- [20:30] 修复高速批量破坏/放置动画不完整不流畅不对齐：`S2CRtsPlace/BreakAnimationPayload` 增加 serverTick+seq 字段，服务端 `RtsPlacementSound`/`RtsMiningNetworkHelper` 按每 tick 每玩家维护序号；客户端 `RtsClientNetworkHandlers` 按 seq 错峰 startMs（25ms/格，cap 96）替代每 tick 20 上限的直接丢弃；`GhostRingBuffer` 新增 `schedule`/`drainPending` 等待队列（满则排队不丢、渲染 pass 每帧补入）并让同位置重复写入保留原播放进度，配套 `GhostRingBufferTest` 新增 3 个用例。
- [21:00] 连锁挖掘启用时禁用破坏模式所有形状：`BuildInteractionHandler.activeBreakShape` 增加 `isUltimineActive` 检查返回 null、`tryStartLineBrush` 重置残留破坏画笔，`ShapeButtonGroup` 新增 `resetModeShape`，`LeftSidebarPanel.render` 连锁挖掘启用时强制破坏侧回落单方块（左键不再被形状画笔抢占，保证触发连锁挖掘）。
- [21:30] 放置/破坏动画改为跟随方块状态变化播放：新增 `client/render/RtsEffectStateTracker`（服务端动画包只登记"预期变化"，客户端捕获到对应状态变化时才写入动画缓冲，同帧对齐 + 顺序容错 + 超时兜底），`ClientPacketListenerMixin` 增加 `handleBlockUpdate` HEAD 注入（`@Shadow getLevel`，读取变化前状态），`RtsClientNetworkHandlers` 的放置/破坏 handler 改用 register，`ClientTickHandler` 挂 `tick()` 超时兜底。曾尝试 `ClientLevelMixin` 注入 `setBlock`+`@Shadow getBlockState` 失败（方法定义在父类 `Level`，dev 无 refMap 无法解析，启动崩溃），已改走 `handleBlockUpdate`。
- [22:00] 连锁挖掘改为按住 `` ` ``（~）键启用：`RtsKeyMappings` 新增 `ULTIMINE_KEY`（默认 GRAVE_ACCENT）+`isUltimineDown()` 按住检测，`RtsKeybinds` 注册 `ultimine` 可自定义；`LeftSidebarPanel.isUltimineActive()` 改读按键状态，下游（BuildInteractionHandler/UltiminePreviewPass/FeatureAdjusters）自动生效。
- [22:30] 连锁挖掘按钮保留为状态指示灯：`UltimineButtonGroup` 恢复（`onButtonClick` 空实现、新增 `setActive`），`LeftSidebarPanel.render` 每帧把按钮点亮同步为按住 `` ` `` 键状态，点击不改变启用；恢复 `tooltip.rtsbuilding.left.ultimine` lang，新增 `key.rtsbuilding.ultimine`。
- [22:45] 动画链路走查修复「动画滞后于方块」：`RtsEffectStateTracker` 重构——动画启动时刻统一锚定到状态就绪/变化瞬间（触发点 `System.currentTimeMillis()`），彻底移除 seq 错峰（原先 25ms/格×seq 最多滞后 1.5s，且跨 tick seq 重置致 startMs 乱序）；register 改为 `registerPlace(pos)`/`registerBreak(pos, beforeState)`；handler 删除 EFFECT_STAGGER 常量；`onBlockChanged`/`tick` 兜底均用触发时刻（原登记 startMs 会因超龄被渲染 pass 跳过）。同步更新 `docs/reports/place-break-render-chain.json` 为状态绑定链路。
- [23:00] 消除动画剩余网络 RTT 滞后：`RtsPlacementSound` 新增带预知 state 的重载 `playRemotePlacedBlockAnimation(player,pos,state)`，`RtsPlacementQuickBuild` 改为在 `setBlock` 之前发放置动画包（动画包先于 BlockUpdate 到达）；`RtsEffectStateTracker.registerPlace` 恢复 targetState 参数、用 `cur.equals(targetState)` 精确匹配（替换模式下旧方块不被误判为已就绪）；`handlePlaceAnimation` 传 `payload.state()`。快速建造路径动画在状态变化时刻零延迟触发，报告 `place-break-render-chain.json` 补第 ⑤ 条问题。
- [23:30] 修复破坏工作流恢复死循环：`RtsToolLeaseManager.borrowMiningTool` 新增 `skipNearBreak` 选项（跳过耐久 ≤5% 工具，阈值与 `isToolNearBreak` 一致），`RtsDestructionBatch.tryResumePendingDestroyJobs` 恢复路径传 true——避免把刚归还的同类型低耐久工具又借回来造成「恢复→挂起」无限循环、作业永远无法恢复；初始借用（ToolBorrowPipe）保持原行为。新增链路报告 `docs/reports/destroy-workflow-resume.json`。
- [23:45] docs 前端（Vue3 SPA）按 DeepSeek 官网风格重写为浅色主题：`style.css` 全面重构（品牌蓝 #4D6BFE→#3982FF→#2498FF 渐变、slate 文字、毛玻璃卡片 backdrop-blur + 柔和多层阴影、绿色/黄色状态徽章、紫色代码高亮）；`App.vue` 新增蓝色渐变 hero 横幅（kicker + 标题 + slogan + 胶囊 Tab，激活白色底蓝字）；`WorkLog.vue` 删除旧深色 scoped style 改为全局浅色样式；`index.html` 补 meta 描述；重新 `npm run build` 生成 `docs/dist/`。
- [23:55] 修复破坏工作流恢复面板「继续」按钮无效：`ResumeWorkflowService.scan`/`apply` 原只查放置作业（`placement.pendingJobs`），破坏作业挂起（`destruction.pendingDestroyJobs`）扫描返回 null、apply 返回 false。新增 `scanDestroyJob`（统计剩余未破坏位置、工具近损坏时置 missingItems=1 使按钮灰化）、`applyDestroyJob`（交 `RtsDestructionBatch.tryResumePendingDestroyJobs` 借还工具并恢复），`DestructionJob` 补 `remainingPositions()`；`destroy-workflow-resume.json` 补第 ② 条问题。
- [00:40] 完整走查工作流生命周期（创建/移除/搁置/继续）并修复三处断点：`RtsWorkflowStatus` 增加 `holdType`（0=运行/1=暂停/2=挂起）并全链路透传（`snapshot`/`S2CRtsWorkflowProgressPayload`/`WorkflowModule`），客户端 `WorkflowInputHandler` 挂起走恢复扫描、暂停/运行走 `PAUSE_WORKFLOW` toggle（修复暂停工作流无法继续）；`ServerActionHandler` 挂起恢复改用 `ResumeWorkflowService.apply` 完整恢复（原只 resume entry 不恢复作业）；`RtsWorkflowEngine.deleteWorkflow` 清理挂起作业队列（防残留误恢复）。新增链路报告 `docs/reports/workflow-lifecycle.json`（问题 4 · 修复 3 · 保留 1，第 ④ 项待日志确认破坏作业挂起定位）。
