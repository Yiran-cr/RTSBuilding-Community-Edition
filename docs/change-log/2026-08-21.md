# 2026-08-21

- [13:23] 放置工作流进度链路检查（第一轮）：移除 `RtsProgressRefresher.refreshPlacementProgress` 世界扫描覆盖（重复计数/误算已有方块/chunk 未加载回退），放置与破坏统一为动作计数驱动（RtsProgressRefresher.java、RtsPlacementBatch.java）。
- [14:10] 放置工作流进度链路检查（第二轮，真正根因）：快速建造/形状放置走 `RtsBlockAnimationCommitter` 延迟落位，但 `tickPlaceBatchJobs` 调度后立即读 afterState 判定成功必然落空 → 全部误计为失败、completed 恒 0。新增 `RtsPlacementQuickBuild.PlaceOutcome` 枚举（CONTINUE/PLACED/SKIPPED/STOP），`placeStateBatchEntry`/`placeSelectedInternal`/`placeReplaceAt` 改为返回调度结果，进度以调度成功为准（RtsPlacementQuickBuild.java、RtsPlacementExecutor.java、RtsPlacementBatch.java）。
- [14:55] 放置工作流进度链路检查（第三轮）：进度改为事件驱动与落位同步——快速建造在 `onCommitFinished(true)` 落位成功才 updateProgress(1)（方块真正出现才计），交互式放置 detectPlacedPos 成功即上报；`placeStateBatchEntry` 增加落位完成回调，`PlaceBatchJob` 增加 pendingPlacements 计数（job 完成需等待落位归零），`RtsBatchJobTickOps` 新增 reportDelta 参数防重复上报（破坏保持差值上报）（RtsPlacementQuickBuild.java、RtsPlacementBatch.java、RtsBatchJobTickOps.java、RtsDestructionBatch.java）。
- [14:55] 更新链路检查报告 `docs/reports/placement-progress-sync.json`。
- [16:30] 新增界面状态持久化基建：`client/state/RtsUiStateStore`（仿 RtsKeybinds 的 JSON 原子读写，`config/rts_building/ui_state.json`），持久化浮动面板位置/尺寸/开启、侧边栏宽高与分区分隔、调试浮层/射线剔除/反转平移/灵敏度开关；BuilderScreen init 应用、onClose 收集保存；给 TopBarPanel/DebugMenuPopup/RightSidebarPanel/DownSidebarPanel 补 getter/setter（RtsUiStateStore.java、BuilderScreen.java、TopBarPanel.java、DebugMenuPopup.java、RightSidebarPanel.java、DownSidebarPanel.java）。
- [17:10] 扩展 UI 状态持久化到全部面板与按钮开关：调色盘改恢复开启；新增顶栏模式、调试子开关×2、明暗主题、渲染动画/深度/透明度、射线剔除距离/半径、漏斗半径/连锁上限、存储过滤×4、左栏点击/建造侧/动作按钮×3/形状×2 等开关与数值的恢复与收集；给 AbstractButtonGroup 补 setSelected、ShapeButtonGroup 补 setModeShape、LeftSidebarPanel 补 applyPersistedState 与形状索引 getter、ItemGrid/RightDownOverlayLayer/DownSidebarPanel 补 GridState 暴露（AbstractButtonGroup.java、ShapeButtonGroup.java、LeftSidebarPanel.java、DebugMenuPopup.java、TopBarPanel.java、ItemGrid.java、RightDownOverlayLayer.java、DownSidebarPanel.java、BuilderScreen.java）。
- [17:45] UI 状态持久化补全侧边栏：SidebarState 增加左栏宽度 leftWidth（原只有右栏宽/下面板高/分区分隔）；BuilderScreen.tick 新增每 200 tick 定时 collect+save 兜底，面板拖拽/拉伸与侧边栏尺寸调整后即使异常退出（不经过 onClose）也最多 10 秒落盘（RtsUiStateStore.java、BuilderScreen.java）。
- [18:20] 侧边栏/面板尺寸持久化加固：BuilderScreen.mouseDragged 与 mouseReleased 均增加 500ms 节流的即时 collect+save，拖拽/拉伸过程与结束即落盘（不再只依赖 onClose/tick）；onClose 与 applySavedUiState 增加 sidebar 保存/恢复值日志便于定位（BuilderScreen.java、RtsUiStateStore.java）。
- [19:00] 排查侧边栏尺寸恢复失效：确认 ui_state.json 保存正常（rightWidth/downHeight 为拖拽后真实值）；新增 ensureUiStateApplied 在 render 首帧兜底恢复，init() 若异常也能在渲染时恢复；确认 LeftSidebarLayoutHelper.SIDEBAR_WIDTH=0 使 leftWidth=0 属正常默认（BuilderScreen.java）。
- [19:40] 侧边栏持久化根因修复：确认 clamp 基准不一致——BuilderScreenScaleManager.enterFixedRtsGuiScale 在鼠标事件期间把 screen.width/height 临时改写为 RTS 虚拟坐标，导致拖拽保存的 currentWidth 基于「虚拟宽/4」clamp，而 init 恢复时 setCurrentWidth 基于「GUI 宽/4」clamp，RTS 缩放≠GUI 缩放时保存值被恢复截断到默认；修复：Right/Down/LeftSidebarPanel 的 setCurrentWidth/setCurrentHeight clamp 上限统一改用 BuilderScreen.getRtsVirtualWidth()/Height()/4；另 applySavedUiState 分段 try-catch 隔离异常、onClose 先 ensureUiStateApplied 再收集，避免未恢复实例把默认尺寸写回文件（RightSidebarPanel.java、DownSidebarPanel.java、LeftSidebarPanel.java、BuilderScreen.java）。
- [20:10] 浮动面板位置/尺寸持久化根因修复：UiPanel.setOpen(true) 原无条件调用 initializeDefaultBounds()，每次从关闭状态打开面板都会把位置重置为默认居中、尺寸重置为默认，导致持久化恢复的位置在用户打开面板时被覆盖；改为仅当 bounds 未初始化（needsInit）时才初始化默认位置，已恢复/已调整的面板打开时保留当前位置与尺寸（UiPanel.java）。
- [20:40] 补全物品网络（ItemGrid/GridState）按钮状态持久化：新增排序类型 grid_sort_type（SortType ordinal）、排序方向 grid_reverse_sort、近期排序方向 grid_recent_sort_asc 的恢复与收集（原只有物品/流体/双向/仅提取 4 个过滤开关）（BuilderScreen.java）。
- [21:05] 方块剔除圆柱预览 pass 改为仅鼠标悬浮于「方块剔除」调节器时显示：FeatureAdjusters 记录剔除复合框命中矩形并提供 isMouseOverCullingAdjuster()（经 LowerRightOverlayLayer/RightSidebarPanel/BuilderScreen 转发）；CylinderCullingPreviewPass 快照仍持续更新（剔除功能保持跟随相机），但线框仅在悬浮时绘制（FeatureAdjusters.java、LowerRightOverlayLayer.java、RightSidebarPanel.java、BuilderScreen.java、CylinderCullingPreviewPass.java）。
- [21:30] 移除无人机碰撞箱显示：新增 EntityRenderDispatcherMixin 拦截 EntityRenderDispatcher.renderHitbox（本地实体）与 renderServerSideHitbox（服务端实体）两个碰撞箱渲染静态方法，对 RtsDroneEntity 直接取消，F3+B 碰撞箱显示时看不到无人机箱子（方法签名经 javap 从 neoforge-21.1.219-merged.jar 确认）（EntityRenderDispatcherMixin.java、rtsbuilding.mixins.json）。
- [21:50] 修复 EntityRenderDispatcherMixin 崩溃：回调方法未加 static 导致 Mixin 报「non-static callback targets a static method」FATAL 崩溃（debug.log 17:24 那次启动）；两处回调改 static 后编译通过（EntityRenderDispatcherMixin.java）。
