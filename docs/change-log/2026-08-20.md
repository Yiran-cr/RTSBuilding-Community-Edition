# 2026-08-20

- [12:44] 新增逻辑：蓝图库面板「使用」按钮仅蓝图模式（BuilderMode.BLUEPRINT）下显示与可用，非蓝图模式按钮不渲染、点击不响应（BlueprintLibraryPanel：isBlueprintMode/computeRowRects 动态布局）。
- [13:50] 蓝图放置模式新增操控：R 键 Y 轴旋转（placementYSteps 循环 0-3）、小键盘方向键按相机朝向水平偏移锚点（placementOffset 持久叠加）；幽灵预览 pass 应用 rotateAroundCenter+rotateState 与服务端放置一致（BuilderScreen/BuilderScreenEventRouter/BlueprintPlacementPreviewPass）。
- [16:32] 蓝图放置交互改为右键两段式：第一次右键定点锁定锚点（pinnedAnchorBase，Esc 先解除定点），第二次右键确认建造；左键点击世界消费防误触；lang 更新 use_hint 并新增 pin_hint（BuilderScreen/BuilderScreenEventRouter/zh_cn/en_us）。
- [17:00] 修复崩溃：BlueprintLibraryPanel.handleContentClick 在非蓝图模式下 btnIdx 无条件自增导致 rowRects.get(4) 越界（IndexOutOfBoundsException），改为 showUse 分支内自增，渲染与点击布局索引恢复一致。
- [17:05] 蓝图放置模式新增滚轮调整锚点高度：adjustBlueprintPlacementHeight 修改 placementOffset 的 Y（定点/非定点均生效），裸滚轮在放置模式下被消费避免与相机缩放冲突；use_hint 文案同步更新（BuilderScreen/BuilderScreenEventRouter/zh_cn/en_us）。
- [17:17] 蓝图放置冲突提示：目标位置已存在不可替换方块时虚影染红（新增 RtsTintVertexConsumer 染色 RGB+alpha，蓝图放置预览 pass 按 canBeReplaced 判定冲突；色块回退分支同样红色偏置）。
- [18:20] 新增右面板「蓝图放置」调节器：覆盖开关（allowOverwrite 走 C2S→BlueprintContext/BlueprintTickPipe.canStillPlace 允许替换，BlueprintPersistence 持久化）+ XYZ 三轴偏移输入框（±64 钳位，写 BuilderScreen.placementOffset）；FeatureAdjusters 新增蓝图框渲染与交互、网关/ServerActionHandler 携带 overwrite 参数；lang 新增 5 个 adjuster key。
- [18:40] 蓝图放置高度调整从裸滚轮改为 Ctrl+滚轮：BuilderScreenEventRouter 滚轮分支加 isCtrlDown，裸滚轮恢复为相机远近缩放；use_hint 文案同步。
- [18:50] 蓝图放置定点/确认从右键两段式改为左键两段式（第一次左键定点、第二次左键确认），右键仍消费防误触；BuilderScreen 注释与 use_hint/pin_hint 文案（zh/en）同步更新。
- [19:10] 蓝图放置操作键位加入 RTS 按键设置：新增 6 个可配置 KeyMapping（定点/确认默认左键、旋转默认 R、四向偏移默认小键盘 8/2/4/6，RtsKeyMappings + RtsKeybinds 注册）；BuilderScreenEventRouter 鼠标/键盘分支改为按绑定匹配（定点键改绑键盘键时鼠标不触发、改由按键分支处理）；lang 新增 6 个 key。- [20:15] 新增射线圆柱视觉剔除：顶栏第 3 按钮 + Y 键开关，圆柱沿准星射线隐藏方块（RtsRayCylinderCullingState 快照节流 + RtsRayCylinder 几何 + 6 个 mixin 拦截 vanilla/Sodium/Embeddium/Flywheel + RtsCullingRenderInvalidator 网格重建）；CylinderCullingPreviewPass 画圆柱线框预览；右面板 FeatureAdjusters 加距离/半径调节器；lang 新增 6 key；新增 RtsRayCylinderTest 几何单测（全部通过）。
- [20:40] 修复崩溃：RtsFlywheelCullingCompat.syncBounds 缺少 ModList.isLoaded('flywheel') 守卫，未装 Flywheel 时加载 FlywheelAccess 内部类抛 NoClassDefFoundError 崩溃于渲染管线（与参考实现 syncBox 的 findActiveFlywheelLevel 守卫对齐）；新增 isFlywheelLoaded() 供 syncBounds/syncBlock 使用。
- [21:05] 修正圆柱轴：CylinderCullingPreviewPass 由 alloc.cursorRay()（鼠标准星随 NDC 偏移）改用 CursorRaycaster.computeCameraCenterRay（摄像机朝向/屏幕中心视线），圆柱轴与鼠标位置无关；同步更新 RtsRayCylinderCullingState 注释与 zh/en lang 的 ray_culling.desc 文案。
- [21:30] 剔除按钮独立化：ray_culling 从 UtilityButtonGroup 拆出为独立 RayCullingButtonGroup（顶栏最右单独一钮，与辅助显示组隔 GROUP_GAP 分开）；TopBarLayoutHelper.GroupLayout 增加 rayCullingGroup 字段（utility 恢复 2 钮）；TopBarPanel 渲染/tooltip/点击三处接入；ray_culling 图标补注册进 RtsMipmapTextures（此前漏注册，HQ 采样纹理不完整）。
- [21:40] 剔除距离最小值 16 → 3（RtsRayCylinderCullingState.MIN_DISTANCE），右面板距离滑块最小值同步生效。
- [22:00] 剔除流畅度优化：①网格重建加 1 格位移阈值（movedBeyondThreshold），相机插值/滚轮微调的亚格位移不再每 250ms 全区域重编译；②新增 EVER_INVALIDATED 历史区域记录，关闭剔除时恢复开启期间所有失效过的区域（含相机移动经过的中间位置），消除网格残留隐藏。
- [22:10] 剔除默认值调整：DEFAULT_DISTANCE 48→5、DEFAULT_RADIUS 6→3（RtsRayCylinderCullingState），右面板滑块初始位置同步生效。
- [22:20] 修复剔除按钮直角问题：uifw 单按钮组默认 bgType=1（圆角 0 直角）；RayCullingButtonGroup 构造改为 bgTypeForButton=-1 走 drawButtonBg default 全圆角分支，仅影响本按钮。
- [22:40] 剔除穿透：①CursorRay.raycastBlock 剔除开启时改为 0.5 格步进跳过圆柱体内方块（命中段 sub-clip 取精确表面点），放置/挖掘/交互射线可穿透被剔除方块；②FreeCameraMode.computeDollyDamping 循环推进跳过剔除方块，滚轮缩进相机不再被剔除方块阻尼阻挡。
- [22:55] 剔除调节器合并：FeatureAdjusters 新增 renderCullingRowBox 复合框（标题「方块剔除」+ 距离/半径两行，每行=小标签+滑块+输入框），替代原来两个独立条目；lang 新增 ui.rtsbuilding.adjuster.culling（zh/en 278 一致）。
- [23:10] 剔除改为直线双向：RtsRayCylinder 轴线段改为「相机位置±方向×距离」（前后各剔除 distance 格），contains/bounds 同步改双向并更新 javadoc；CylinderCullingPreviewPass 预览线框双向绘制；RtsRayCylinderTest 新增 bidirectionalCulling、后方边界/垂直下方用例、bounds 后端断言；lang 描述更新（zh/en 278 一致）。
